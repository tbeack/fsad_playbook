# CBP-158 — Update `/resume` Cheat Sheet row: background session support (v2.1.144)

**Status:** done

## What changed
Claude Code v2.1.144 added `/resume` support for background sessions. Sessions started via `claude --bg` or Agent View now appear alongside interactive sessions in the resume picker, marked with a `bg` label.

## Change made
Updated the `/resume` row in the Cheat Sheet table to append:
> Background sessions (started via `--bg` or Agent View) now appear alongside interactive sessions in the picker, marked with a `bg` label.
