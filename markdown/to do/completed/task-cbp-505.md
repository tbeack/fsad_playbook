# CBP-505 — Rework Harness Engineering section diagrams to match source reference + turn checklist into an actual checklist

## Source
User-supplied source reference: `file:///Users/theobeack/Downloads/harness engineering.html` (the same saved X/Twitter thread by @0xwhrrari used as CBP-500's content source), specifically its two embedded diagram images:
- `harness engineering_files/HQ1oAGxWsAAttaT.jpg` — **"SEVEN JOBS OF THE AGENT HARNESS"**: a horizontal 7-box pipeline (01/CONTRACT–DEFINE, 02/CONTEXT–SELECT, 03/TOOLS–ACT, 04/STATE–REMEMBER, 05/SENSORS–VERIFY, 06/POLICY–AUTHORIZE, 07/TRACES–EXPLAIN), each box with an icon, a sub-label (e.g. "goal · limits · done"), connected by arrows, closed with a summary bar: "ONE MODEL CALL IS AN EVENT · THE HARNESS IS THE SYSTEM" / "LEGIBLE BEFORE ACTION → CONTROLLED DURING ACTION → AUDITABLE AFTER ACTION."
- `harness engineering_files/HQ1pxJvWAAAXXOY.jpg` — "FAILURE BECOMES INFRASTRUCTURE" (not in scope for this task; already conceptually covered by the existing `#failure-upgrades` section).

## Summary
Three targeted changes to `src/pages/harness.html` (added in CBP-500):
1. Rework the first diagram (in `#model-vs-harness`) so its embedded seven-job list uses the source's exact terminology and structure instead of the current paraphrased labels, restyled to the playbook's own visual system (not the source's minimalist black-line style).
2. Add a brand-new diagram — a horizontal 7-box pipeline mirroring the source's "Seven Jobs of the Agent Harness" image — placed directly below the `<h2 class="section-title">What a Production Harness Actually Does</h2>` heading (and its subtitle) in `#seven-jobs`, styled consistently with diagram 1's rework.
3. Convert the 12-item `#checklist` section from plain `<div class="card"><p>...</p></div>` statements into an actual interactive checklist (real checkboxes with persisted checked state).

## Assessment

**Current state, verified by direct inspection of `src/pages/harness.html`:**

- **`#model-vs-harness` (lines 10–49)** — hero section "The Model Proposes. The Harness Decides." containing a `<figure class="harness-flow-diagram">` with dual dark/light SVGs (`viewBox 0 0 680 482`). Layout: a top banner, a left MODEL panel (4 bullet facts), an arrow labeled "proposes," and a right HARNESS panel listing seven job rows as flat text: "(a) Turn request into a contract," "(b) Give the agent a map, not a manual," "(c) Expose tools with clear contracts," "(d) Externalize memory into durable state," "(e) Add sensors before autonomy," "(f) Enforce permissions outside the model," "(g) Record traces, recover locally" — a bottom closing banner. None of this uses the source's short-form terminology (CONTRACT/CONTEXT/TOOLS/STATE/SENSORS/POLICY/TRACES paired with DEFINE/SELECT/ACT/REMEMBER/VERIFY/AUTHORIZE/EXPLAIN, plus each job's compact sub-label like "goal · limits · done").
- **`#seven-jobs` (lines 50–151)** — heading "What a Production Harness Actually Does" + subtitle, then seven `.collapsible` blocks (`(a)` through `(g)`), each with prose detail. **No diagram anywhere in this section today.**
- **`#checklist` (lines 274–308)** — heading "The Harness Engineering Checklist," a `.card-grid` of 12 `<div class="card"><p>...</p></div>` items, each a plain self-assessment question with no interactivity. No existing checklist/checkbox CSS pattern exists anywhere in `src/styles.css` or `src/js/` — this will be new.

**Style reference already established in this file** (both diagrams and the maturity-ladder diagram at lines 244–273 follow the same convention, to be matched by the two reworked/new diagrams):
- `<figure class="{name}-flow-diagram" aria-label="...">` wrapping two `<svg class="flow-diagram flow-diagram--dark">` / `flow-diagram--light` variants (light shown via `[data-theme="light"] .{name}-flow-diagram .flow-diagram--light { display:block; }` in `src/styles.css`).
- `<title>`/`<desc>` on every SVG for accessibility, describing the diagram in prose.
- Font: `'Anthropic Sans',-apple-system,'Segoe UI',sans-serif`. Dark palette: background `#252b38`/`#251f2c`, accent `#8a82dd`, text `#e8eaef`/`#9aa3b4`, success green `#4cc9a0`/`#14241e`/`#6dd9b3`. Light palette: background `#ffffff`/`#efecf9`, accent `#534ab7`, text `#1f2937`/`#5b6577`, success green `#2f9e78`/`#e6f5ef`/`#1f7a5c`. Rounded rects (`rx 12–24`), arrow `<marker>`s per theme.

**Interpretation note (flagging the judgment call so it's checkable later):** the user's brief names "the first diagram" (item 1) and "an image to mirror... the seven jobs of a harness" (item 2) as two distinct asks. Read literally: item 1 reworks the *existing* `#model-vs-harness` diagram's seven-job list to adopt the source's terminology while keeping its own two-panel Model/Harness concept; item 2 is a *new*, separate horizontal pipeline diagram (closer to a direct restyle of the source image) placed in `#seven-jobs`. If in doubt while implementing, this split — terminology-only rework in place #1, full pipeline diagram net-new in place #2 — is the intended reading.

## Plan

1. **Rework diagram 1** (`#model-vs-harness`, both dark and light `<svg>` blocks): replace the seven flat text rows in the HARNESS panel with the source's paired terminology — number + short name (01/CONTRACT ... 07/TRACES), the one-word verb (DEFINE ... EXPLAIN), and the compact sub-label (goal · limits · done, etc.) — reflowed to fit the existing panel's vertical row layout. Update the `<title>`/`<desc>` text to describe the new terminology. Keep the existing two-panel Model/Harness structure, banners, and playbook color system unchanged.
2. **Add diagram 2** to `#seven-jobs`: a new `<figure class="harness-jobs-flow-diagram">` with dual dark/light SVGs, directly after the section's `<h2>`/subtitle and before the first `.collapsible`. Horizontal 7-box pipeline, each box: number/name header (e.g. "01 / CONTRACT"), an icon (reuse/adapt simple stroke icons consistent with the playbook's existing icon language, not the source's literal icon set), the verb (DEFINE), and the sub-label (goal · limits · done) — connected by arrows, closed with a summary bar reflecting the source's closing lines adapted to the playbook's voice. Match diagram 1's exact color/font/gradient system for visual consistency between the two.
3. **Convert the checklist**: replace each `<div class="card"><p>...</p></div>` in `#checklist`'s `.card-grid` with a checkbox-driven item (`<input type="checkbox" id="harness-check-N">` + `<label for="harness-check-N">`), styled via new CSS in `src/styles.css` (checked state gets a visual treatment — e.g. accent border/checkmark, consistent with the playbook's existing card/collapsible aesthetic). Add a small JS module (new file under `src/js/`, following the existing fragment-per-concern pattern, wired into `build-source.py`'s JS assembly) that persists checked state to `localStorage` keyed per-item and restores it on page load, so a visitor's progress survives a reload — same persistence pattern already used by the theme toggle.
4. **Build + verify**: run `python3 scripts/build-source.py` then `python3 scripts/build-dist.py` (confirm `Injected PLAYBOOK_EMBEDDINGS` in the log), open the built file in a browser, and visually check both diagrams and the checklist in **both** dark and light themes (no clipped/overlapping text, legible contrast, checkboxes toggle and persist across reload).
5. Stage `src/` and `dist/` together in the same commit (per this repo's build+commit requirement) — commit only when explicitly asked to.

## Acceptance Criteria
- [x] Diagram 1 in `#model-vs-harness` (both dark and light SVG variants) presents the seven harness jobs using the source's exact terminology (CONTRACT/CONTEXT/TOOLS/STATE/SENSORS/POLICY/TRACES paired with DEFINE/SELECT/ACT/REMEMBER/VERIFY/AUTHORIZE/EXPLAIN and their short sub-labels), restyled with the playbook's own color/font/gradient system rather than the source's plain black-line style.
- [x] A new horizontal 7-box pipeline diagram (dark + light SVG variants), mirroring the source's "Seven Jobs of the Agent Harness" image and matching diagram 1's restyled visual language, is inserted directly below the `<h2>`/subtitle of `#seven-jobs` ("What a Production Harness Actually Does"), before the first collapsible.
- [x] The `#checklist` section's 12 items render as real, independently toggleable checkboxes (not plain `<p>` cards), each with an associated `<label>`.
- [x] Checked/unchecked state for each checklist item persists across a full page reload (verified via `localStorage`).
- [x] `scripts/build-source.py` and `scripts/build-dist.py` both run without error, the dist build logs `Injected PLAYBOOK_EMBEDDINGS`, and both `fsad-playbook.html` and `dist/fsad-playbook.html` reflect all three changes.
- [x] Both diagrams and the checklist render legibly in both dark and light themes (no clipped text, no low-contrast elements) — verified in a live browser check, not just by reading the SVG markup. **Caveat:** the two diagrams were confirmed via successful pixel screenshots in both themes. The checklist section sits ~6,800–7,600px deep on this ~8,755px-tall page, where the claude-in-chrome screenshot tool consistently returns a blank/black capture regardless of theme (reproduced independently by two separate verification passes, dozens of workaround attempts each — waits, zoomed regions, incremental scroll, window resize, fresh tabs — all failed the same way). This is a capture-tool limitation at extreme scroll depth, not a page defect: computed-style inspection confirms `opacity:1`/`visibility:visible` on all 12 items in both themes, zero clipped labels (`scrollWidth/Height == clientWidth/Height`), and WCAG-AA-passing contrast ratios (8.63:1 light, 5.55:1 dark, both well above the 4.5:1 threshold). Accepted as sufficient evidence per user decision (2026-09-01).

All criteria verified 2026-09-01 before commit.
