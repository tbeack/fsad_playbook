# 2026-05-07 — Claude Code v2.1.132

## New Features
- Added `CLAUDE_CODE_SESSION_ID` env var to Bash tool subprocess environment, matching the `session_id` passed to hooks → **Affects:** Claude Best Practices / Hooks (env vars in hooks table)
- Added `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` env var to opt out of fullscreen alternate-screen renderer → **Affects:** Claude Best Practices / Power Usage (Subprocess Sandboxing env vars table)

## Fixed
- Fixed external SIGINT not running graceful shutdown — terminal modes restored and `--resume` hint printed → **Affects:** No playbook impact
- Fixed `--resume` failing with emoji surrogate error; pre-corrupted sessions sanitized on load → **Affects:** No playbook impact
- Fixed `--permission-mode` flag ignored when resuming a plan-mode session → **Affects:** No playbook impact
- Fixed fullscreen blank screen after laptop sleep/wake or Ctrl+Z/fg → **Affects:** No playbook impact
- Fixed cursor mid-grapheme on Ctrl+E/A/K/U/arrow keys with Indic conjuncts → **Affects:** No playbook impact
- Fixed vim operators corrupting NFD accented characters → **Affects:** No playbook impact
- Fixed pasting text starting with `/` swallowing input → **Affects:** No playbook impact
- Fixed pasting dumping stray escape sequences → **Affects:** No playbook impact
- Fixed mouse wheel scrolling too fast in Cursor/VS Code → **Affects:** No playbook impact
- Fixed scroll-wheel handling in JetBrains IDE 2025.2 terminals → **Affects:** No playbook impact
- Fixed `/usage` Ctrl+S hanging on Linux/X11 → **Affects:** No playbook impact
- Fixed `/terminal-setup` contradictory error in Windows Terminal → **Affects:** No playbook impact
- Fixed `/effort` picker not reflecting `CLAUDE_CODE_EFFORT_LEVEL` env var override → **Affects:** No playbook impact
- Fixed `/status` showing wrong default model → **Affects:** No playbook impact
- Fixed slash command autocomplete popup capped at 3–5 commands → **Affects:** No playbook impact
- Fixed statusline context_window token counts reflecting cumulative totals → **Affects:** No playbook impact
- Fixed Alt+T (thinking toggle) not working on macOS terminals → **Affects:** No playbook impact
- Fixed dead keyboard input on Windows after re-opening background session → **Affects:** No playbook impact
- Fixed unbounded memory growth (10GB+ RSS) when stdio MCP server writes non-protocol data to stdout → **Affects:** No playbook impact
- Fixed MCP servers with failed tools/list showing 0 tools silently — now retry once and show "connected · tools fetch failed" → **Affects:** No playbook impact
- Fixed unauthorized claude.ai MCP connectors showing "failed" instead of "needs auth" → **Affects:** No playbook impact
- Fixed Bedrock and Vertex 400 errors when `ENABLE_PROMPT_CACHING_1H` is set → **Affects:** No playbook impact

## Playbook Action Items
- [x] CBP-118: Add `CLAUDE_CODE_SESSION_ID` to hooks env vars table
- [x] CBP-119: Add `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` to Subprocess Sandboxing env vars table

---

# 2026-05-07 — Codex CLI v0.129.0 (was rust-v0.128.0)

## New Features
- Vim modal editing in composer (`/vim`, `vim_mode` config, Vim-specific keymap contexts) → **Affects:** Codex Cheat Sheet + Codex Power Usage
- Redesigned resume/fork picker, raw scrollback mode, `/ide` context injection, workspace-aware `/diff` → **Affects:** Codex Cheat Sheet + Codex Power Usage (Session Management)
- Hooks can be browsed and toggled from `/hooks` TUI → **Affects:** Codex Cheat Sheet
- Hooks can run before/after compaction (PreCompact, PostCompact events) → **Affects:** Codex Hooks event types table + comparison table
- `PreToolUse` hooks can add context → **Affects:** Codex Hooks event types table
- `/keymap debug` for inspecting terminal key events → **Affects:** Codex Cheat Sheet
- Plugin workspace sharing, share access controls, remote bundle sync → **Affects:** Codex Power Usage Plugins collapsible
- Experimental goals: discoverable, paused across resume, multi-day duration → **Affects:** Codex Cheat Sheet + Codex Power Usage Goals collapsible

## Changed
- Codex Apps auth and eligible MCP elicitations surface through TUI/Guardian flows → **No playbook impact**
- Linux releases bundle standalone `bwrap` fallback → **No playbook impact**

## Fixed
- Various TUI, terminal, sandbox reliability fixes → **No playbook impact**

## Playbook Action Items
- [x] CBP-120 — `/vim` slash command + `vim_mode` config + Vim Editing Mode collapsible
- [x] CBP-121 — `/hooks` slash command + PreCompact/PostCompact events + context injection note
- [x] CBP-122 — Session Management picker + raw scrollback + `/ide` + `/diff` workspace-aware
- [x] CBP-123 — `/keymap debug` + Plugins workspace sharing
- [x] CBP-124 — Goals: discoverable + paused-across-resume + multi-day duration
