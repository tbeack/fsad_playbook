# 2026-05-05 — Claude Code v2.1.128

## New Features
- Bare `/color` (no args) picks a random session color → **Affects:** Cheat Sheet (Configuration table)
- `/mcp` shows tool count per server and flags 0-tool servers → **Affects:** Cheat Sheet (Configuration table)
- `--plugin-dir` accepts `.zip` plugin archives in addition to directories → **Affects:** Power Usage (Plugins collapsible)

## Changed
- Subprocesses (Bash, hooks, MCP, LSP) no longer inherit `OTEL_*` env vars → **Affects:** Monitoring section (Distributed Tracing bullet)
- MCP `workspace` is now a reserved server name (skipped with warning) → **Affects:** Power Usage (Plugins collapsible)
- `EnterWorktree` branch creation fixed: uses local HEAD instead of `origin/<default-branch>` → **Affects:** No playbook impact (internal behavior fix, no docs needed)
- SDK hosts receive persistent `localSettings` suggestion for Bash permission prompts → **Affects:** No playbook impact (internal SDK behavior)
- `/model` picker: collapsed duplicate Opus 4.7 entries → **Affects:** No playbook impact (UI-only)
- Auto mode classifier error now includes retry hint → **Affects:** No playbook impact (error message improvement)
- MCP reconnect no longer floods conversation with tool-name lists → **Affects:** No playbook impact (internal behavior fix)

## Fixed
- 20+ bug fixes including: crash on large stdin, drag-and-drop hang, focus mode flicker, Kitty OSC 9 notification, parallel shell tool cancellation, MCP stdio corruption, prompt-too-long on 1M-context models, etc. → **Affects:** No playbook impact (bug fixes)

## Playbook Action Items
- [x] CBP-100: Update `/color` Cheat Sheet row to mention random-color behavior
- [x] CBP-101: Update `--plugin-dir` Plugins collapsible to mention `.zip` support
- [x] CBP-102: Update Distributed Tracing bullet re: OTEL var inheritance isolation
- [x] CBP-103: Add `workspace` reserved MCP server name warning to Plugins collapsible
- [x] CBP-104: Update `/mcp` Cheat Sheet row with tool count and 0-tool flagging
