# 2026-08-21 — Claude Code v2.1.239

## Claude Code Changes

### New Features
- Added `/claude-api upgrade` to migrate Python projects from `anthropic` 0.x to 1.x, and updated the `claude-api` skill's Python reference for 1.x (timeouts use `anthropic.Timeout`, not `httpx.Timeout`)
- Windows: cross-session messaging is now available, so Claude Code sessions across your machines can message each other with `SendMessage` and find each other with `ListAgents`, as on macOS and Linux
- Cost estimates (`/cost`, status line, `--max-budget-usd`) now include the 1.1x US-only-inference premium for data-residency workspaces
- Added the one-time fullscreen renderer offer on Bedrock, Vertex, Foundry and other previously excluded setups; new installs there now start in fullscreen

### Changed
- Cloud sessions: plugins synced from claude.ai now show as `name@synced`, work with `claude plugin enable/disable <name>@synced`, and never override a same-named plugin you installed
- `keybindingFlavor: "readline"` now also matches Bash for word keys: Alt+F and Ctrl/Option+→ stop at the end of the word, Alt+D deletes to it (Ctrl+Y pastes it back), and punctuation separates words
- Persistent retry mode (`CLAUDE_CODE_RETRY_WATCHDOG`) now fails immediately on organization spend-limit and out-of-credits errors instead of waiting indefinitely for a reset
- `/goal`: repeat check-ins on long-running background work now back off (30 min, then 1 h, then every 2 h) instead of repeating every 30 minutes
- `/goal`: resuming a session from the `claude --resume` picker now restores its active goal
- `ListAgents` now tells a session its own name (the one peers use to message it), and `SendMessage` to your own name says so instead of "no agent named …"
- `ListAgents` and `/list-agents` now list your live teammates (previously only subagents and other sessions appeared, so a reachable teammate looked absent)
- Alpine/musl builds: native image paste, clipboard, and audio-capture add-ons now load
- The usage-limit message shown when your monthly spend limit is already used up now also says when your session or weekly limit resets

### Fixed
- Fixed Bedrock streaming behind proxies that strip the response Content-Type header, which silently doubled billed API calls
- Fixed Claude Code hanging at startup behind an HTTPS proxy when using Bedrock with an SSO profile and `awsAuthRefresh`
- Fixed a raw crash dump when starting Claude Code from a directory that no longer exists
- Fixed Edit and Write calls pausing for about 5 seconds in JetBrains IDE terminals when the Claude Code plugin is connected
- Fixed a race where pressing Esc with a prompt queued could let the next turn finish early
- Fixed WebFetch retaining expired page content in memory for the whole session instead of the intended 15 minutes
- Fixed cloud sessions resuming out of plan mode after an idle worker restart
- Fixed MCP elicitation forms taller than the terminal being clipped in fullscreen mode
- Fixed remote MCP servers staying failed after a transient 5xx on a mid-session reconnect
- Fixed custom session titles disappearing from `/resume` after more than ~64 KB of conversation
- Fixed `claude -c`/resume picking up sessions from a different directory whose path differed only by punctuation characters
- Fixed `/resume` and the agents view showing a session as recently changed when only its file was touched
- Fixed `/resume` in all-projects mode telling you to `cd` into a deleted directory
- Fixed the `dark-ansi` theme rendering expanded tool results in fullscreen mode with unreadable text color
- Fixed the fullscreen renderer prompt reappearing on every launch; it now stops after three launches
- Fixed `.worktreeinclude` patterns starting with `**/` silently matching nothing in gitignored directories
- Fixed agents, skills, and commands whose `.md` file starts with a UTF-8 BOM being silently ignored
- Fixed `/insights` echoing literal `<message>` tags on some models
- Fixed marketplace `metadata.pluginRoot` having no effect
- Fixed mouse movement in browser-based terminals inserting stray text into the prompt
- Fixed custom theme overrides for the effort/ultracode status badge colors being ignored
- Fixed OpenTelemetry trace fragmentation: tool executions deferred by a `PreToolUse` hook now resume in the original turn's trace
- Fixed vim mode in the agent view: Escape now switches to NORMAL mode and keeps text
- Fixed the `selection:copy` keybinding dropping a Shift+Arrow-extended selection
- Fixed the `/voice` startup tip still appearing after voice dictation was enabled via settings
- Fixed shell-mode (`!`) Tab completion dropping the `./` from a `./script` path
- Fixed fullscreen mode reacting to a click that was only meant to refocus the terminal window
- Fixed slash-command panels covering the latest messages in fullscreen mode
- Fixed the `/workflows` detail dialog overflowing the terminal
- Fixed the Linux sandbox making a nonexistent `.git/config.worktree` unreadable
- Fixed hooks failing with "posix_spawn ENOENT" after the session's working directory was deleted
- Fixed `claudeMdExcludes` not excluding a symlinked `.claude/rules` file
- Fixed runaway session-title syncing to Remote Control (2.1.232 regression)
- Fixed sessions whose title starts with `/` being unaddressable by `SendMessage`
- Fixed several keybindings leaving a broken `[Pasted text #N]` placeholder
- Fixed masked (password-style) inputs letting their text be pasted back or saved to prompt history
- Fixed Ctrl+Backspace deleting one character instead of a word in search boxes
- Fixed a request rejected by an organization policy check being re-sent before the rejection was shown
- Various Remote Control reliability and messaging fixes

## Playbook Action Items
- [ ] `CBP-430`: Add `/claude-api upgrade` to Cheat Sheet Info & account table
- [ ] `CBP-431`: Extend `keybindingFlavor: "readline"` Ctrl+W row for Bash-word-key matching
- [ ] `CBP-432`: Extend `/goal` row — resuming a session restores its active goal
- [ ] `CBP-433`: Add bullet to Plugins collapsible for claude.ai-synced plugins shown as `name@synced`
- [ ] `CBP-434`: Extend Agent Teams cross-session `SendMessage`/`ListAgents` bullet — Windows parity + live-teammates fix
- [ ] `CBP-435`: Extend `CLAUDE_CODE_RETRY_WATCHDOG` row — fails immediately on org spend-limit/out-of-credits errors
- [ ] `CBP-436`: Extend `/usage` row — cost estimates include 1.1x US-only-inference premium for data-residency workspaces

## No Action (assessed, not actionable)
- Fullscreen renderer one-time offer extended to Bedrock/Vertex/Foundry — no action, because this is an onboarding-only mechanic (first-launch prompt); no existing playbook section documents the "offer" flow itself, so there is nothing stale to correct, and the underlying fullscreen-mode content is unaffected.
- OpenTelemetry trace fragmentation fix (deferred `PreToolUse` tool executions now resume the original trace) — no action, because the Hooks Deep Dive section documents the `defer` decision but never described trace/span continuity behavior; an internal telemetry bug fix with nothing previously (mis)documented to fix.
- `/claude-api upgrade` skill's Python reference update (timeouts now use `anthropic.Timeout` not `httpx.Timeout`) — no action beyond CBP-430 (which documents the command itself); this is an internal SDK-reference detail one level deeper than the playbook's Building Skills section covers, which doesn't reference the built-in `claude-api` skill's Python internals at all.
- Alpine/musl builds: native image paste, clipboard, and audio-capture add-ons now load — no action, because this is a platform-packaging fix with no corresponding "Alpine/musl setup" guidance anywhere in the playbook to update.
- Usage-limit message now also states session/weekly reset timing — no action, minor UX copy clarification with no playbook claim it contradicts.
- The ~35 "Fixed" bug-fix entries in this release (Bedrock streaming/proxy issues, startup crash dump, JetBrains Edit/Write pause, Esc-race, WebFetch cache TTL, cloud-session plan-mode resume, MCP elicitation clipping, remote MCP reconnect, session-title/`/resume` bugs across several rows, `dark-ansi` theme fullscreen contrast, fullscreen-renderer-prompt repeat, `.worktreeinclude` matching, BOM-prefixed agent/skill/command files, `/insights` tag echo, marketplace `pluginRoot`, mouse-report text injection, custom theme badge colors, vim-mode Escape, `selection:copy` keybinding, `/voice` tip, shell-mode Tab completion, fullscreen refocus-click, slash-command panel overlap, `/workflows` dialog overflow, Linux sandbox `.git/config.worktree`, hook `posix_spawn ENOENT`, `claudeMdExcludes` symlink matching, runaway session-title sync, `/`-prefixed session titles, several keybinding/paste-placeholder bugs, masked-input leakage, Ctrl+Backspace, org-policy-rejection re-send, and various Remote Control reliability fixes) — no action, because these are internal bug fixes correcting behavior that was never itself documented in the playbook as a feature or caveat; there is no stale claim in `fsad-playbook.html` for any of them to invalidate.

## Deferred
(none — every actionable change from this run's release delta was turned into a CBP task above)

## Execution Gaps
(none — all 7 CBP tasks (CBP-430 through CBP-436) were applied to `fsad-playbook.html` and the resulting diff passed Phase 5.5 independent verification with a PASS verdict)
