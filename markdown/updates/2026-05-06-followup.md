# 2026-05-06 — Deferred Items Follow-Up (CBP-114–CBP-115)

## Status
No new Claude Code or Codex releases detected.
- Claude Code: v2.1.131 (unchanged)
- Codex CLI: rust-v0.128.0 (unchanged)

## Work Done
Processed two items deferred from the 2026-05-06 v2.1.131 run:

### CBP-114: skillOverrides setting
Added `skillOverrides` to the Notable `settings.json` Keys callout in Config Cascade.
Three modes documented: `off`, `user-invocable-only`, `name-only`.

### CBP-115: Plugin manifest experimental nesting
Updated the `monitors` frontmatter row to note that in plugin `manifest.json` files,
`monitors` and `themes` should be declared under `"experimental": { ... }` to avoid
`claude plugin validate` warnings. Skill SKILL.md frontmatter is unaffected.

## Playbook Version
v47 → v48
