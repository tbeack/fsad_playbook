# 2026-09-01 — Claude Code v2.1.252 / Codex CLI v0.152.0

## Claude Code Changes

### Fixed
- Fixed Bash commands failing with "task output swap refused (tasks dir moved or linked)" on some Macs
- Fixed "always allow" not saving in a project that has no `.claude/settings.local.json` yet
- Fixed Remote Control sessions hosted by Claude Desktop or VS Code stalling for minutes after a tool finished when the connection to claude.ai was degraded
- Fixed background task notifications with very large failure output (e.g. git errors on a full disk) making the conversation exceed the API request size limit

(v2.1.252 is a bug-fix-only patch release — no new features, no playbook action items.)

## Codex CLI Changes

### New Features
- Vim mode supports `/` and `?` searches within drafts, highlighted matches, and repeat navigation with `n`/`N`
- Rate-limit banners offer actions for checking usage, managing credits, resetting limits, and managing plans
- The terminal UI and `codex exec` show credential-refresh progress, including Amazon Bedrock reauthentication
- MCP server names can contain `:`, `@`, `/`, and `.`, supporting package-style names throughout CLI commands and authentication
- Individual MCP tools support an `output_token_limit` setting, with consistent truncation across session resumes
- App-server clients can configure `thread/shellCommand` timeouts, including deadlines longer than one hour

### Changed
- The planning tool is disabled by default; enable it with `tools.update_plan.enabled = true`
- Plugin recommendations begin loading during startup, reducing delays before the first turn

### Fixed
- Vim-enabled composers now start fresh drafts in Insert mode
- Automatic approval reviews retain longer messages/transcripts and preserve instructions/answers/authorizations across history compaction
- Resumed threads restore their saved working directory when none is supplied
- MCP tools remain available through cache refreshes and remote plugin changes
- Opening the model picker refreshes available models without losing the highlighted choice
- Fixed Windows sandbox execution with Microsoft Store PowerShell, subprocess hangs on terminal queries, cursor-related display corruption in older JediTerm terminals
- Cloud task requests reject untrusted backend URLs and disable redirects
- Various other internal fixes (see full changelog: https://github.com/openai/codex/compare/rust-v0.151.0...rust-v0.152.0)

## Playbook Action Items
- [ ] CBP-501: [Codex] Document Vim mode `/` and `?` search within drafts (rust-v0.152.0)
- [ ] CBP-502: [Codex] Document rate-limit banner actions on `/usage` row (rust-v0.152.0)
- [ ] CBP-503: [Codex] Document per-MCP-tool `output_token_limit` + resume-consistent truncation (rust-v0.152.0)
- [ ] CBP-504: [Codex] Add `tools.update_plan.enabled` config row — planning tool disabled by default (rust-v0.152.0)

## Deferred
- App-server `thread/shellCommand` client timeout configuration (rust-v0.152.0) — deferred: this is a protocol-level config surface for third-party clients building on the Codex app-server API, not a CLI end-user behavior. The playbook's existing app-server coverage (`#codex-code-review`, line 540) is a one-line pointer to external docs by design; a deeper protocol config detail doesn't fit any existing anchor group's scope without expanding the playbook's audience beyond CLI practitioners. No CBP task opened.
- MCP server names supporting `:`, `@`, `/`, `.` characters (rust-v0.152.0) — logged as no-action rather than deferred: this widens internal server-name validation, not a user-facing configuration decision documented anywhere in the playbook.
- Terminal UI credential-refresh progress display, including Bedrock reauthentication (rust-v0.152.0) — logged as no-action: cosmetic progress indicator during an existing, already-documented auth flow; no new configuration or behavior to record.
- Plugin recommendation preloading during startup (rust-v0.152.0, Chores) — logged as no-action: internal performance improvement with no user-visible configuration surface.
