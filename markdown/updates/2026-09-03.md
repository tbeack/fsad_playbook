# 2026-09-03 — Claude Code v2.1.259 / Codex rust-v0.153.0

## Claude Code v2.1.259

### New Features
- Added `managedMcpServers` managed setting: orgs can provide HTTP/SSE MCP servers to every user via org/MDM policy → **Affects:** Config Cascade / Notable `settings.json` Keys (New addition)
- Added `--permission-prompts none` for unattended headless hosts: anything that would prompt is denied automatically → **Affects:** Cheat Sheet CLI flags (New addition)
- Added recognition of `glab mr create/merge/close/reopen/note/update` → **Affects:** No playbook impact (already covered in agents table)
- Added `--json` to `claude plugin validate` → **Affects:** No playbook impact (internal tooling)

### Changed
- Changed `allowedMcpServers` to govern only servers users add; `deniedMcpServers` now needed to block managed servers → **Affects:** MCP section (no current entry to update — managedMcpServers task covers context)
- [VSCode] Added Active filter and status filter to session list → **Affects:** No playbook impact (VSCode UI only)

### Fixed
- Numerous bug fixes (worktree isolation, prompt cache, concurrent sessions, stop hooks, etc.) → **Affects:** No playbook impact (stability improvements)

### Claude Code Playbook Action Items
- [x] CBP-510 Add `--permission-prompts none` flag to Cheat Sheet CLI flags table — New row
- [x] CBP-511 Document `managedMcpServers` org-wide MCP managed setting — New list item in Notable settings.json Keys

## Codex rust-v0.153.0

### New Features
- Vim mode now supports undo (`u`) and redo (`Ctrl+R`), preserving complete drafts → **Affects:** Codex Vim section (Update existing)
- Plugin CLI can list, install, and remove plugins from remote marketplaces (`codex plugin list`) → **Affects:** Codex Plugins section (Update existing)
- Set `tui.auto_recap = false` to disable automatic recaps while keeping manual `/recap` available → **Affects:** Config Reference table (New row)
- TUI history shows complete patches, terminal input, individual commands → **Affects:** No playbook impact (display improvement)
- Early rate-limit warning for Plus/Team plans → **Affects:** No playbook impact (UI notification)

### Configuration and API Updates
- `tui.disable_paste_burst` replaces top-level setting (remains as fallback) → **Affects:** No playbook impact (minor rename, backward-compatible)
- `features.context_management.experimental_mode` — disabled-by-default experimental feature for ChatGPT sessions → **Affects:** No playbook impact (experimental, ChatGPT-sessions only)

### Fixed
- TUI reconnect after app-server disconnect → **Affects:** No playbook impact
- Full Access skips Guardian reviews → **Affects:** No playbook impact
- Various MCP approval scoping fixes → **Affects:** No playbook impact

### Codex Playbook Action Items
- [x] CBP-512 Update Vim mode section to add undo (`u`) and redo (`Ctrl+R`) — Update existing
- [x] CBP-513 Add `codex plugin list` and note remote marketplace CLI support — Update existing
- [x] CBP-514 Add `tui.auto_recap` to config.toml reference table — New row
