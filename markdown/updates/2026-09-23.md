# 2026-09-23 — Codex rust-v0.156.0 / rust-v0.156.1

Claude Code: v2.1.280 (unchanged).

## Codex rust-v0.156.0 / rust-v0.156.1

### New Features

- `/tui` — optional fullscreen UI with transcript search, mouse selection, and right-click copying → **Affects:** Codex Cheat Sheet (new `/tui` row), Codex Power Usage (new TUI section or update existing)
- Voice conversations enabled by default — F8 toggle, `/voice settings` picker, bundled audio runtimes for Linux and Windows; no longer requires `/experimental` → **Affects:** Codex Cheat Sheet `/voice` row, Voice Conversations collapsible
- `/usage` analytics dashboard — now also tracks plugin and skill activity (in addition to token totals) → **Affects:** Codex Cheat Sheet `/usage` row (minor update)
- Agent command center — filter tasks by status, create worktree sessions from the dashboard; worktree support now enabled by default (no longer experimental) → **Affects:** Codex Cheat Sheet `/worktree` row, Worktree Sessions collapsible
- Six new terminal themes + Mermaid diagram rendering + display equation support → **Affects:** No playbook impact (cosmetic/rendering feature)
- `/daemon` slash command — update the local background server through `/daemon`; bypass it with `--no-daemon` CLI flag → **Affects:** Codex Cheat Sheet (new `/daemon` row), CLI Flags table (new `--no-daemon` row)
- GPT-6 Sol and Luna added to model picker; rate-limit switch prompt recommends GPT-6 Luna (rust-v0.156.1) → **Affects:** Multi-Provider Models table (OpenAI row)

### Changed

- Rate-limit switch now recommends GPT-6 Luna instead of the previous default → **Affects:** Multi-Provider Models table note

### Fixed / Removed

- Preserve streamed answers and plans when turns fail or are interrupted → No playbook impact
- Clipboard forwarding in tmux and SSH sessions fixed → No playbook impact
- Restore Plan mode when resuming sessions → No playbook impact
- Various sandbox isolation fixes → No playbook impact

### Codex Playbook Action Items

- [x] CBP-591 Update `/voice` cheat sheet row + Voice Conversations collapsible — voice is now on by default (no longer experimental), F8 toggle, `/voice settings` picker, bundled runtimes for Linux/Windows
- [x] CBP-592 Add `/tui` cheat sheet row — fullscreen UI with transcript search, mouse selection, right-click copying
- [x] CBP-593 Update `/worktree` cheat sheet row + Worktree Sessions collapsible — remove "experimental" label, note worktree is now enabled by default, mention task filter by status in agent command center
- [x] CBP-594 Add `/daemon` cheat sheet row + `--no-daemon` CLI flag row
- [x] CBP-595 Update OpenAI row in Multi-Provider Models table — add GPT-6 Sol and Luna; note rate-limit switch recommends Luna
