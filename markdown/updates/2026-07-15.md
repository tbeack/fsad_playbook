# 2026-07-15 — Claude Code v2.1.210

## New Features
- Added a live elapsed-time counter to the collapsed tool summary line → **Affects:** No playbook impact (UI-only change)
- Added a startup warning for `Write(path)`, `NotebookEdit(path)`, and `Glob(path)` permission rules — use `Edit(path)` or `Read(path)` instead → **Affects:** Claude Best Practices / Notable settings.json Keys callout

## Changed
- Improved auto mode: the permission classifier now defaults to Sonnet 5 for external sessions, validated on the session's first request and pinned for the session → **Affects:** Claude Best Practices / Notable settings.json Keys callout (autoMode section)
- Hardened the Agent tool against indirect prompt injection via content a subagent read → **Affects:** No playbook impact (internal security hardening)
- Memory writes that leave a MEMORY.md index over its read limit now produce an explicit error instead of silent truncation → **Affects:** No playbook impact (error messaging improvement)
- Screen reader mode now announces permission mode changes aloud when cycling modes with Shift+Tab → **Affects:** No playbook impact (screen reader UX)

## Fixed
- `isolation: 'worktree'` subagents being able to run git-mutating commands against the main repo checkout → **Affects:** No playbook impact (bug fix, existing behavior now correct)
- `ultracode` keyword opt-in firing on non-human-originated input such as webhook payloads → **Affects:** No playbook impact
- Paste markers leaking into external editors (stray È/É characters) → **Affects:** No playbook impact
- `claude attach` failing with "job not found" / "agent is still starting" → **Affects:** No playbook impact (bug fix)
- Hook callback timeout misreported as user rejection, making unattended sessions wait → **Affects:** No playbook impact (bug fix — existing hook docs are correct)
- `claude agents --effort ultracode` not reaching dispatched sessions → **Affects:** No playbook impact
- Unmatched `$1`/`$2` positional placeholders in skills and commands silently stripped — now preserved verbatim → **Affects:** No playbook impact (existing docs at line 9238 already document `$0`/`$1` correctly; this is a bug fix)
- `/doctor` skipping auto-mode-default proposal on Bedrock, Vertex, and Foundry → **Affects:** No playbook impact (bug fix; `/doctor` docs already current)
- Killed background sessions leaving permanent `git worktree lock` → **Affects:** No playbook impact
- Plugin-provided MCP servers being torn down when MCP servers are re-synced mid-session → **Affects:** No playbook impact
- Various other crash/UI fixes → **Affects:** No playbook impact

## Claude Code Playbook Action Items
- [x] CBP-305 — Add startup warning note for `Write(path)`, `NotebookEdit(path)`, and `Glob(path)` permission rules to Notable settings.json Keys callout
- [x] CBP-306 — Add autoMode classifier Sonnet 5 default note for external sessions to Notable settings.json Keys callout

## Codex
Codex rust-v0.144.4 (unchanged from last check — no updates needed).
