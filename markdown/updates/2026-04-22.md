# 2026-04-22 — Claude Code v2.1.117

## New Features
- Forked subagents enabled on external builds via `CLAUDE_CODE_FORK_SUBAGENT=1` → **Affects:** No playbook impact (external builds only)
- Agent frontmatter `mcpServers` loaded for main-thread agent sessions via `--agent` → **Affects:** Building Skills (frontmatter reference table)
- Native macOS/Linux: Glob and Grep tools replaced by embedded bfs/ugrep via Bash tool → **Affects:** Power Usage / Building Skills (tool mental model)
- `plugin install` on already-installed plugin now installs missing dependencies → **Affects:** No playbook impact
- `cleanupPeriodDays` retention sweep now covers `~/.claude/tasks/`, `~/.claude/shell-snapshots/`, `~/.claude/backups/` → **Affects:** No playbook impact
- Advisor Tool (experimental): labeled "experimental", startup notification → **Affects:** No playbook impact (too experimental to document)

## Changed
- Default effort for Pro/Max subscribers on Opus 4.6 and Sonnet 4.6 is now `high` (was `medium`) → **Affects:** Claude Best Practices (Model & Effort section + Cheat Sheet + CLI flags)
- `/model`: selections now persist across restarts; startup header shows when model comes from project/managed-settings pin → **Affects:** Cheat Sheet (`/model` row description)
- `/resume` now offers to summarize stale, large sessions before re-reading → **Affects:** Cheat Sheet (`/resume` row description)
- OpenTelemetry: `user_prompt` events now include `command_name`/`command_source`; `cost.usage` etc. include `effort` attribute → **Affects:** Claude Monitoring (OTEL section) — low priority, queue for next run
- Managed-settings `blockedMarketplaces` / `strictKnownMarketplaces` enforcement expanded → **Affects:** No playbook impact (enterprise admin feature)

## Fixed
- OAuth token refresh on 401 → No playbook impact
- WebFetch truncation → No playbook impact
- Ctrl+_ undo → No playbook impact
- Opus 4.7 `/context` percentage fix (was computing against 200K instead of 1M) → No playbook impact
- Various bug fixes → No playbook impact

## Playbook Action Items
- [x] CBP-051 — Update default effort for Pro/Max on Opus 4.6/Sonnet 4.6: now `high` — Update existing Model & Effort callout, Cheat Sheet, CLI flags
- [x] CBP-052 — Update `/model` Cheat Sheet row: selections persist across restarts + startup shows pin source — Update existing
- [x] CBP-053 — Update `/resume` Cheat Sheet row: now summarizes stale large sessions — Update existing
- [x] CBP-054 — Add `mcpServers` to skill frontmatter reference table — New row
- [x] CBP-055 — Note native Glob/Grep → bfs/ugrep replacement in relevant section — Update existing

## Queued for Next Run (lower priority)
- OTEL `command_name`/`command_source` and `effort` attribute additions to OTEL section
