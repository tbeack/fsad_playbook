# 2026-09-13 — Claude Code v2.1.270

## Claude Code Changes

### Fixed
- Read-only git commands in Bash unexpectedly asking for permission after a session had been running for a while (regression in 2.1.269).

## Playbook Action Items
(none — single internal bugfix, no user-facing feature/setting/workflow to document)

## No Action (Claude Code)
- v2.1.270 is a one-line regression fix for a permission-prompt bug introduced in 2.1.269. No new commands, flags, settings, or workflow changes — nothing for the playbook to cover.

## No Action (Codex CLI)
- Not checked this run (task scope was Claude Code CLI only; `lastCodexVersion`/`lastCodexChecked` in state.json left as-is).
