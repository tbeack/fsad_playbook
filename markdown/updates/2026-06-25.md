# 2026-06-25 — Claude Code v2.1.186–v2.1.191

## New since v2.1.185

- **v2.1.186** — `claude mcp login/logout <name>` CLI, `respondToBashCommands` setting, `!` bash auto-response, background subagent permission prompts in main session, `CLAUDE_CODE_MAX_RETRIES` cap at 15, `CLAUDE_CODE_RETRY_WATCHDOG` for unattended sessions
- **v2.1.187** — `sandbox.credentials` setting, org-configured model restrictions, `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`, `/install-github-app` workflow now optional
- **v2.1.190** — Bug fixes and reliability improvements (no playbook items)
- **v2.1.191** — `/rewind` resume after `/clear`, hooks comma-separated matcher fix, sandbox network permission host memory

## Playbook Action Items (CBP-264 through CBP-268)

- **CBP-264** — `/mcp` Cheat Sheet row: `claude mcp login/logout <name>` CLI commands
- **CBP-265** — Notable settings.json: `respondToBashCommands` setting
- **CBP-266** — Sandbox settings table: `sandbox.credentials` setting
- **CBP-267** — `/rewind` Cheat Sheet row: resume after `/clear`
- **CBP-268** — Hardening env vars: `CLAUDE_CODE_RETRY_WATCHDOG`
