# 2026-06-22 — Claude Code v2.1.186 / Codex rust-v0.142.0

## New Features (Claude Code v2.1.186)

- Added `claude mcp login <name>` and `claude mcp logout <name>` CLI subcommands to authenticate MCP servers without opening the interactive `/mcp` menu; `--no-browser` redirects to stdin for SSH flows → **Affects:** Claude Best Practices — Cheat Sheet (MCP row)
- Added `teammateMode: "iterm2"` setting → **Affects:** No playbook impact (niche terminal backend config)
- Added "Skills" section to `/plugin` Installed tab → **Affects:** No playbook impact (UI navigation refinement)
- Added status filtering (`f`) to `/workflows` agent detail view → **Affects:** No playbook impact (UI refinement)
- `!` bash commands now trigger Claude to respond to output automatically; opt out with `respondToBashCommands: false` in settings.json → **Affects:** Claude Best Practices — Notable settings.json Keys callout
- Changed `CLAUDE_CODE_MAX_RETRIES` to cap at 15; for unattended sessions use `CLAUDE_CODE_RETRY_WATCHDOG` instead → **Affects:** Claude Best Practices — Power Usage / hardening env vars

## Changed

- Improved skill frontmatter: `display-name`, `default-enabled`, `fallback`, and `metadata.*` keys now accept kebab-case, snake_case, and camelCase → **Affects:** Claude Best Practices — Building Skills frontmatter reference
- Changed background subagents to surface permission prompts in main session instead of auto-denying → **Affects:** No playbook impact (behavior change, no new config surface)
- Changed `/review <pr>` to use same engine as `/code-review medium` → **Affects:** No playbook impact (internal routing)

## Fixed

- Fixed `Agent(type)` deny rules and `Agent(x,y)` allowed-types restrictions not enforced for named subagent spawns → **Affects:** No playbook impact (bug fix)
- Fixed Workflow `agent({schema})` subagents looping forever on repeated schema validation failures → **Affects:** No playbook impact (bug fix)
- Fixed streaming requests failing after machine wakes from sleep → **Affects:** No playbook impact (bug fix)
- Multiple UI and background agent fixes → **Affects:** No playbook impact

## Claude Code Playbook Action Items

- [x] CBP-266 — Add `claude mcp login/logout` CLI commands to Cheat Sheet MCP row
- [x] CBP-267 — Add `respondToBashCommands` to Notable settings.json Keys callout
- [x] CBP-268 — Add `CLAUDE_CODE_RETRY_WATCHDOG` + updated `CLAUDE_CODE_MAX_RETRIES` cap to hardening env vars table
- [x] CBP-269 — Update skill frontmatter note: kebab-case/snake_case/camelCase accepted for `display-name`, `default-enabled`, `fallback`, `metadata.*`

---

## Codex rust-v0.142.0

### New Features

- Rollout token budgets: configurable per-agent-thread budget that tracks usage, provides remaining-budget reminders, and aborts turns when exhausted → **Affects:** Codex Best Practices — Codex Power Usage (new section needed)
- `/usage` can now show and redeem earned usage-limit reset credits → **Affects:** No playbook impact (subscription UI)
- `/plugins` organizes remote plugins into OpenAI Curated, Workspace, and Shared-with-me sections; eligible turns can recommend and install plugins automatically → **Affects:** No playbook impact (plugins UI refinement)
- App-server clients can configure multi-agent delegation (disabled / explicit-request-only / proactive) at thread and turn level → **Affects:** No playbook impact (app-server API, not CLI)
- Indexed web-search mode: live searches allowed while restricting direct page access to server-approved URLs → **Affects:** No playbook impact (app-server feature, not CLI config surface)
- Codex can receive scheduled UTC time reminders and query current time via app-server clocks → **Affects:** No playbook impact (app-server / ChatGPT integration feature)

### Bug Fixes

- Restored Linux TUI rendering after `Ctrl+Z` suspend/resume → **Affects:** No playbook impact (bug fix)
- Exec-server processes and stdio MCP sessions survive transient disconnects → **Affects:** No playbook impact (infrastructure)
- Remote environments preserve executor-native paths, shells, AGENTS.md discovery → **Affects:** No playbook impact (remote env infrastructure)
- Parent agents now receive terminal subagent errors instead of silent empty success → **Affects:** No playbook impact (bug fix)

### Codex Playbook Action Items

- [x] CBP-270 — Document rollout token budgets in Codex Power Usage section
