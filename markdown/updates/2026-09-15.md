# 2026-09-15 — Claude Code v2.1.271/v2.1.272 Auto-Update

Checked Claude Code and Codex releases. Claude Code: latest is v2.1.272 (recorded `lastVersion` was v2.1.270 — two releases behind: v2.1.271 and v2.1.272). Codex: rust-v0.154.0 (unchanged).

## Changelog Assessment

**v2.1.272** — "Bug fixes and reliability improvements" only, no changelog detail beyond that one line. No playbook-relevant content.

**v2.1.271** — substantial release. Selected six items with clear, direct playbook relevance (concrete new flags/settings/behavior changes with an existing home in the playbook's structure); skipped items that were either pure UI polish (mouse support in `/config`, spinner tip wording, single QR code for `/mobile`), enterprise-billing-only (`modelPricing` multiplier), too niche for this playbook's scope (`--drain-marker-file` telemetry-only runner flag), or plain bug fixes with no user-facing behavior to document.

### Tasks Created and Executed

- **CBP-558** [Claude] Per-command `allowed_domains` for Bash/PowerShell/Monitor in auto mode — Subprocess Sandboxing collapsible
- **CBP-559** [Claude] `omitClaudeMd` agent frontmatter — Frontmatter Reference collapsible
- **CBP-560** [Claude] `--accept-command <sha256>` plugin install/update flag — Plugins collapsible
- **CBP-561** [Claude] Lowered default Dynamic Workflow size (Pro plans default to `small`; `medium` guideline 15→10) — Dynamic Workflows collapsible
- **CBP-562** [Claude] Monitor watch deadline change (always bounded, replacing no-timeout `persistent`) — Monitor Tool collapsible
- **CBP-563** [Claude] Fast mode in Remote sessions (cloud and self-hosted runners) — Remote Control & Cross-Device collapsible

All six edits landed in `src/pages/practices.html`. Version bumped to v4.1.24 across the three tracked locations (title tag, sidebar-brand badge, changelog modal) plus `README.md` and `CHANGELOG.md`.

## No Updates (Codex re-check, later run same day)

Re-ran the check: Claude Code latest is still v2.1.272 (matches `lastVersion`, no action), Codex CLI latest is still rust-v0.154.0 (matches `lastCodexVersion`), and the OpenAI Codex changelog has no entries newer than rust-v0.154.0. No new tasks created.
