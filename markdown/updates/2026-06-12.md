# 2026-06-12 — Claude Code v2.1.175

## New Features
- Added `enforceAvailableModels` managed setting — when enabled, the `availableModels` allowlist also constrains the Default model (a Default that would resolve to a disallowed model now falls back to the first allowed model), and user or project settings can no longer widen a managed `availableModels` list → **Affects:** Claude Best Practices / Config Cascade / Notable settings.json Keys

## Changed
- No changes with playbook impact

## Fixed (v2.1.174)
- `wheelScrollAccelerationEnabled` setting added — disable mouse-wheel scroll acceleration in fullscreen mode → **Affects:** Claude Best Practices / Config Cascade / Notable settings.json Keys
- `/model` picker now shows Opus as its own row on Max/Team Premium/Enterprise plans → **Affects:** No playbook impact (UI fix)
- Fixed Bedrock GovCloud regions deriving wrong inference profile prefix → **Affects:** No playbook impact (internal fix)
- Fixed background sessions inheriting another session's `ANTHROPIC_*` env → **Affects:** No playbook impact (internal fix)
- Fixed 1-2 second pause when exiting Claude Code after interrupted shell commands → **Affects:** No playbook impact (UX fix)
- Fixed git commit co-author attribution showing incorrect model name → **Affects:** No playbook impact (internal fix)
- Fixed `/advisor` dialog pre-selecting a blocked model → **Affects:** No playbook impact (UI fix)
- Fixed skill hot-reload re-sending entire skill listing when a single skill changed → **Affects:** No playbook impact (performance fix)
- Fixed Workflow tool `agent()` subagents missing per-agent attribution headers → **Affects:** No playbook impact (internal fix)
- Fixed pre-warmed background workers failing with auth error → **Affects:** No playbook impact (internal fix)
- [VSCode] Added usage attribution breakdown to Account & usage dialog → **Affects:** No playbook impact (VSCode-only feature)

## Playbook Action Items
- [x] CBP-250: Add `enforceAvailableModels` to Notable settings.json Keys callout — New entry
- [x] CBP-251: Add `wheelScrollAccelerationEnabled` to Notable settings.json Keys callout — New entry
