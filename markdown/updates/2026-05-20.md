# 2026-05-20 — Claude Code v2.1.145

## New Features
- Added `claude agents --json` to list live Claude sessions as JSON for scripting (tmux-resurrect, status bars, session pickers) → **Affects:** Cheat Sheet (claude agents row) → CBP-175
- `/plugin` Discover and Browse screens now show a plugin's commands, agents, skills, hooks, and MCP/LSP servers before installation → **Affects:** Cheat Sheet (/plugin row) + Plugins collapsible → CBP-177
- Stop and SubagentStop hook input now includes `background_tasks` and `session_crons` fields → **Affects:** Cheat Sheet (Hooks tables) → CBP-176

## Changed (already covered in v2.1.144 — on main)
- `/resume` background session support → CBP-166 (done)
- `/model` session-only scope + `d` for default → CBP-167 (done)
- `/extra-usage` renamed to `/usage-credits` → CBP-168 (done)

## Changed (v2.1.145 — no playbook impact)
- `claude agents` terminal tab title shows awaiting-input count → UI behavior only
- Slash command and @-mention suggestion list supports mouse hover/click in fullscreen → UI behavior only
- Status line JSON input includes GitHub repo and PR information → internal enhancement

## Fixed (v2.1.145 — no playbook impact)
- Fixed permission-prompt bypass for bare variable assignments in Bash
- Fixed MCP prompt slash commands showing raw validation errors
- Fixed spinner/elapsed-time display freezing after terminal resize
- Fixed cross-project resume hint in Windows PowerShell 5.1
- Fixed Agent Teams non-ASCII name API failures
- Fixed `/review` deprecated GraphQL query
- Fixed `claude plugin validate` not flagging skill entries pointing at a file instead of a directory
- Fixed infinite loop in `context: fork` skills
- Improved Read tool to return truncated first page instead of hard error on token limit

## Playbook Action Items
- [x] CBP-175: Add `claude agents --json` flag to Cheat Sheet
- [x] CBP-176: Add `background_tasks` and `session_crons` to Stop/SubagentStop hook rows
- [x] CBP-177: Update `/plugin` row with pre-install component preview
