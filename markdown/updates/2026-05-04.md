# 2026-05-04 — Claude Code v2.1.126 (follow-up pass)

## Status
Latest Claude Code release is still v2.1.126 (published 2026-05-01). No new version since last check.

## Follow-up Assessment
The previous run (CBP-090–093) covered the headline v2.1.126 features. This run audited v2.1.121–v2.1.126 for items missed in prior passes.

## New Features / Changes Assessed

- `ANTHROPIC_BEDROCK_SERVICE_TIER` (v2.1.122) → **Affects:** Subprocess Sandboxing env vars table
- Type-to-filter search in `/skills` (v2.1.121) → **Affects:** Cheat Sheet `/skills` row
- `/terminal-setup` enables iTerm2 clipboard (v2.1.121) → **Affects:** Cheat Sheet `/terminal-setup` row
- `/model` picker lists gateway `/v1/models` when `ANTHROPIC_BASE_URL` set (v2.1.126) → **Affects:** Cheat Sheet `/model` row

## Playbook Action Items
- [x] CBP-094 — Update `/terminal-setup` row (iTerm2 clipboard)
- [x] CBP-095 — Update `/skills` row (type-to-filter)
- [x] CBP-096 — Add `ANTHROPIC_BEDROCK_SERVICE_TIER` to Subprocess Sandboxing table
- [x] CBP-097 — Update `/model` row (gateway `/v1/models`)

## Outcome
Playbook bumped from v41 to v42. Four Cheat Sheet and Power Usage entries updated.
