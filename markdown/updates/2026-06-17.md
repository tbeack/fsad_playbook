# 2026-06-17 — Claude Code v2.1.179

## Summary

New version detected: v2.1.179 (previously v2.1.178, checked 2026-06-16).
Published: 2026-06-16.

## Changes in v2.1.179

### Fixed
- Fixed mid-stream connection drops: partial responses are now preserved instead of showing a raw error, and the spinner no longer gets stuck at "running tool" → **Affects:** No playbook impact (bug fix)
- Fixed mouse-wheel scrolling in WSL2 under Windows Terminal and VS Code (regression in 2.1.172) → **Affects:** No playbook impact (platform bug fix)
- Fixed a sandbox `denyRead`/`allowRead` glob over a large directory tree making the Bash tool description enormous and the session unusable on Linux → **Affects:** No playbook impact (`denyRead`/`allowRead` already documented; this is a performance bug fix)
- Fixed the feedback survey capturing a single-digit reply as a session rating immediately after a turn completes → **Affects:** No playbook impact (UI bug fix)
- Fixed the welcome screen stacking multiple promotional banners — at most one promo now shows per session → **Affects:** No playbook impact (UI bug fix)
- Fixed Ctrl+O not showing the subagent's transcript when viewing a subagent → **Affects:** No playbook impact (UI bug fix)
- Fixed clicking the prompt input not returning focus from the subagent/footer panel → **Affects:** No playbook impact (UI bug fix)
- Fixed remote session background tasks appearing stuck as "still running" between turns → **Affects:** No playbook impact (bug fix)
- Improved plugin loading performance in remote sessions → **Affects:** No playbook impact (performance improvement)

## Playbook Action Items

None. v2.1.179 is a pure bug-fix release with no new features, commands, flags, settings, or user-facing behaviors that require documentation updates.
