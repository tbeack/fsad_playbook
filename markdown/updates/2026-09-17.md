# 2026-09-17 — Claude Code v2.1.274

## New Features
- Added `CLAUDE_CODE_MCP_STARTUP_WAIT_MS` to bound how long the first non-interactive turn waits for connecting MCP servers (`0` = don't wait) → **Affects:** Subprocess Sandboxing hardening env vars table
- Added a visible warning when memory usage is critical, with steps to free memory or restart safely → **Affects:** No playbook impact (in-product UX, no setting/flag)
- Added `effort` attribute to the `claude_code.llm_request` OpenTelemetry trace span, matching the `api_request` event → **Affects:** No playbook impact (telemetry field, not a documented metric row)
- Added `claude_code.managed_settings_resolved` OTel event; `store.connect_timeout_seconds` gateway config; `enduser.sub` gateway telemetry; gateway replica-limit warning → **Affects:** No playbook impact (Claude apps gateway deployment internals, out of this CLI playbook's scope)
- Added click-to-expand for collapsed teammate and agent messages in fullscreen mode → **Affects:** No playbook impact (minor UI)

## Changed
- Changed `/code-review` to use leaner inline review prompts for every model that has no tuned settings of its own, instead of spawning many review subagents → **Affects:** `/code-review` Cheat Sheet row
- Changed Bash permission checks for commands that loop over or assign certain special shell variables; these commands now ask for permission → **Affects:** Subprocess Sandboxing collapsible opening paragraph
- Changed worktree-isolated sessions to refuse Bash commands with certain nested shell expansions that were previously accepted → **Affects:** Subprocess Sandboxing collapsible opening paragraph
- Changed Bedrock, Vertex, Foundry, and telemetry-disabled installs to use the v2 MCP client and MCP 2026-07-28 negotiation with direct HTTP servers by default (opt out: `MCP_SDK_GENERATION=v1` / `MCP_PROTOCOL_NEGOTIATION=legacy`) → **Affects:** Deferred (see below)
- Changed `"type": "sdk"` MCP entries in configs to be skipped with a warning; artifact-watching no longer starts a turn on a remote publish; plugin/marketplace clones leave Git LFS as pointers; self-hosted runners skip read-only repos at the access check; `/status` GitHub line now reads "Cloud sessions" → **Affects:** No playbook impact (internal/edge-case behavior, no documented surface changes)

## Fixed
- Large batch of reliability fixes (transcript self-healing, MCP HTTP/SSE and Streamable HTTP timeouts, `claude agents` flag loss after auto-update relaunch, per-turn diagnostics slowdown, subagent model fallback on Bedrock/Vertex/Foundry, self-hosted runner 401 retries, AskUserQuestion preview bugs, Artifact tool approval bug, and more) → **Affects:** No playbook impact (bug fixes restoring already-documented or undocumented-broken behavior)
- VS Code, Claude Code on the web, Claude Tag (Slack), and Code Review surface-specific fixes and improvements (window-reload continuation, Memory/Instructions menu, session archiving, cloud-session diff branch picker, Slack mention/threading fixes, code-review finding wording) → **Affects:** No playbook impact (these product surfaces are outside the playbook's CLI-focused scope; none introduce a new CLI command, flag, or setting)

## Claude Code Playbook Action Items
- [x] CBP-568 Add `CLAUDE_CODE_MCP_STARTUP_WAIT_MS` to Subprocess Sandboxing hardening env vars table — Update existing
- [x] CBP-569 Extend Subprocess Sandboxing paragraph with two v2.1.274 permission-check tightenings — Update existing
- [x] CBP-570 Update `/code-review` Cheat Sheet row with leaner inline review prompts note — Update existing

## Deferred
- MCP v2 client + 2026-07-28 negotiation default for Bedrock/Vertex/Foundry/telemetry-disabled installs — deferred: a client-protocol default change with no effect on how users configure or use MCP servers day-to-day; the two opt-out env vars (`MCP_SDK_GENERATION`, `MCP_PROTOCOL_NEGOTIATION`) are niche fallback knobs for protocol regressions, not part of the normal setup flow the MCP section documents. Revisit if a v1→v2 migration issue becomes common enough to warrant a troubleshooting note.
- Cloud-session diff view "Compare against" branch picker — deferred: a Claude Code on the web UI feature with no CLI flag, setting, or command; the playbook's Remote Control & Cross-Device collapsible tracks session/config behavior, not web UI affordances.
- Remaining internal reliability fixes and VS Code / Claude Tag / Code Review surface items — deferred: bug fixes restoring already-expected behavior, or changes to product surfaces (VS Code extension internals, Slack app, code-review bot wording) outside this playbook's CLI/settings/cheat-sheet scope.

## Codex Status
Not checked this run (task scope was Claude Code CLI only; `lastCodexVersion`/`lastCodexChecked` in state.json left as-is).
