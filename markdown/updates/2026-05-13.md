# 2026-05-13 — Claude Code v2.1.140

## New Features
- None

## Changed
- Improved Agent tool `subagent_type` matching to accept case- and separator-insensitive values (e.g. `"Code Reviewer"` resolves to `code-reviewer`) → **Affects:** No playbook impact (internal matching improvement)
- Updated agent color palette → **Affects:** No playbook impact (visual change)

## Fixed
- Fixed `/goal` silently hanging when `disableAllHooks` or `allowManagedHooksOnly` is set — now shows a clear message → **Affects:** Config Cascade Notable settings.json Keys (add `allowManagedHooksOnly`)
- Fixed symlinked settings files causing misattributed change events and spurious `ConfigChange` hooks → **Affects:** No playbook impact (bug fix)
- Fixed `claude --bg` failing with "connection dropped mid-request" when background service was about to idle-exit → **Affects:** No playbook impact (bug fix)
- Fixed background service startup on machines with enterprise endpoint security → **Affects:** No playbook impact (bug fix)
- Fixed remote managed settings not retrying on 401 → **Affects:** No playbook impact (bug fix)
- Fixed managed `extraKnownMarketplaces` auto-update policy not being persisted → **Affects:** No playbook impact (bug fix)
- Fixed `/loop` scheduling redundant wakeups → **Affects:** No playbook impact (bug fix)
- Fixed Windows event-loop stall from `where.exe` re-spawns → **Affects:** No playbook impact (bug fix)
- Fixed `Read` tool offset validation for whitespace-padded or `+`-prefixed strings → **Affects:** No playbook impact (bug fix)
- Fixed native terminal cursor not staying at input caret → **Affects:** No playbook impact (bug fix)
- Plugins now warn when a default component folder (e.g. `commands/`) is silently ignored because `plugin.json` sets the matching key → **Affects:** Plugins collapsible (document the warning + guidance)

## Playbook Action Items
- [x] CBP-137: Add `allowManagedHooksOnly` to Notable settings.json Keys callout in Config Cascade (v2.1.140)
- [x] CBP-138: Document plugin default folder conflict warning in Plugins collapsible (v2.1.140)
