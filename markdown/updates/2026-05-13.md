# 2026-05-13 — Claude Code v2.1.140

## New Features
- Improved Agent tool `subagent_type` matching: now accepts case- and separator-insensitive values (e.g. `"Code Reviewer"` resolves to `code-reviewer`) → **Affects:** No playbook impact — playbook does not document `subagent_type` casing rules.
- Updated agent color palette → **Affects:** No playbook impact — cosmetic change.

## Fixed
- Fixed `/goal` silently hanging when `disableAllHooks` or `allowManagedHooksOnly` is set → **Affects:** No playbook impact — bug fix.
- Fixed settings hot-reload regression: symlinked settings files caused misattributed change events and spurious `ConfigChange` hooks → **Affects:** No playbook impact — bug fix.
- Fixed `claude --bg` failing with "connection dropped mid-request" when background service was about to idle-exit → **Affects:** No playbook impact — bug fix.
- Fixed background service startup failing on machines with enterprise endpoint security → **Affects:** No playbook impact — bug fix.
- Fixed remote managed settings not retrying on 401 → **Affects:** No playbook impact — bug fix.
- Fixed managed `extraKnownMarketplaces` auto-update policy not persisted → **Affects:** No playbook impact — bug fix.
- Fixed `/loop` scheduling redundant wakeups → **Affects:** No playbook impact — bug fix.
- Fixed event-loop stall on Windows (`where.exe` re-spawns) → **Affects:** No playbook impact — bug fix.
- Fixed `Read` tool failing validation when `offset` is passed with whitespace-padding or `+` prefix → **Affects:** No playbook impact — bug fix.
- Fixed native terminal cursor position after terminal loses focus → **Affects:** No playbook impact — bug fix.
- Plugins now warn when `plugin.json` key shadows a default component folder (e.g. `commands/`) → **Affects:** No playbook impact — playbook does not document `plugin.json` component-folder key interaction.

## Playbook Action Items
None. This is a bug-fix release. All 14 changes are either internal bug fixes, cosmetic updates, or improvements to behaviors not currently documented in the playbook.
