# 2026-05-21 — Claude Code v2.1.146

## New Features
- None

## Changed
- Renamed `/simplify` to `/code-review` with an optional effort level (e.g. `/code-review high`) → **Affects:** Cheat Sheet (Slash Commands — Automation & agents)

## Fixed
- Auto mode no longer suppresses `AskUserQuestion` when the user or a skill explicitly relies on it → No playbook impact
- Fixed Windows PowerShell tool failing with "command line is invalid" when `pwsh` is installed via winget or the Microsoft Store → No playbook impact
- Fixed MCP `resources/list`, `resources/templates/list`, and `prompts/list` dropping items past page 1 on paginating servers → No playbook impact
- Fixed full-screen strobing in attached background sessions on Windows Terminal → No playbook impact
- Fixed the auto-updater status line not showing your current version when an update fails → No playbook impact
- Fixed on Windows, removing a background-job worktree no longer follows NTFS junctions into the main repo → No playbook impact
- Fixed `/background` refusing sessions whose only typed input was a skill or custom slash command → No playbook impact
- Fixed backgrounded sessions re-prompting for tool permissions you already granted with "don't ask again" → No playbook impact
- Fixed `/theme` color editor and "New custom theme" dialogs not responding to Esc → No playbook impact
- Fixed an uncaught exception at the end of streaming sessions when running via the Agent SDK → No playbook impact
- Fixed `forceLoginOrgUUID` and `forceLoginMethod` managed-settings policies not being enforced → No playbook impact
- Fixed GNOME Terminal right-click and middle-click paste not inserting text → No playbook impact
- Fixed `CLAUDE_CODE_SUBAGENT_MODEL` not being forwarded to child processes in multi-agent sessions → No playbook impact (already documented)
- Improved auto-updater reliability: native version checks and downloads now retry transient network failures → No playbook impact
- Improved diff rendering performance for large file edits → No playbook impact

## Playbook Action Items
- [ ] CBP-180: Add `/code-review [effort]` to Cheat Sheet slash commands table (Automation & agents) — renamed from `/simplify`, supports optional effort level (v2.1.146)
