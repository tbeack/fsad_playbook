# 2026-05-21 — Claude Code v2.1.146

## Context

State on entry: `lastVersion: v2.1.145`. Main playbook is at v2.68.0 with CBP-179 as the highest task.

## New in v2.1.146

- `/simplify` renamed to `/code-review` with optional effort level (e.g., `/code-review high`) → **Affects:** Cheat Sheet (new row needed — command not present in playbook)
- Auto mode no longer suppresses `AskUserQuestion` when skill relies on it → No playbook impact
- Windows PowerShell fix — No playbook impact
- MCP pagination fix — No playbook impact
- Various other bug fixes — No playbook impact
- Auto-updater reliability (retry transient failures) — No playbook impact
- Diff rendering performance improvement — No playbook impact

## Carried over from v2.1.145 (missed in CBP-175–177)

- `agent_id` and `parent_agent_id` added to `claude_code.tool` OTEL spans; trace parenting fixed → **Affects:** Monitoring section OTEL Events table (no `claude_code.tool` row exists)

## Playbook Action Items

- [x] CBP-180: Add `/code-review [effort]` to Cheat Sheet — renamed from `/simplify`, optional effort level (v2.1.146)
- [x] CBP-181: Add `claude_code.tool` OTEL span row with `agent_id`/`parent_agent_id` to Monitoring events table (v2.1.145, carried)
