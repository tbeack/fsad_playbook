# 2026-04-29 — Claude Code v2.1.123

State at last check: `lastVersion: v2.1.119`, `lastChecked: 2026-04-27`. Four new releases since then (v2.1.120, v2.1.121, v2.1.122, v2.1.123). The 2026-04-28 update file assessed v2.1.120 and v2.1.121 but those tasks (CBP-082–086) were never executed. This run executes all five and also folds in v2.1.122 and v2.1.123.

## New Features

### v2.1.123
- Fixed OAuth authentication failing with 401 retry loop when `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1` is set. → **Affects:** No playbook impact (bug fix)

### v2.1.122
- `ANTHROPIC_BEDROCK_SERVICE_TIER` env var to select Bedrock service tier (`default`, `flex`, `priority`). → **Affects:** Bedrock-specific enterprise config — no playbook impact
- Pasting a PR URL into `/resume` search box finds the session that created that PR (GitHub, GitHub Enterprise, GitLab, Bitbucket). → **Affects:** Cheat Sheet (`/resume` row) — **actioned in CBP-082**
- `/mcp` shows claude.ai connectors hidden by a manually-added server with the same URL. → **Affects:** No playbook impact (UX hint)
- OpenTelemetry: numeric attributes now emitted as numbers, not strings. → **Affects:** No playbook impact
- OpenTelemetry: `claude_code.at_mention` log event added. → **Affects:** OTEL table — minor; deferred
- Multiple bug fixes (Vertex AI, Bedrock ARNs, proxy gateways, spinner tips, ToolSearch MCP timing, `!exit` in bash mode, image resize, tmux redraw, blank messages, malformed hooks). → **Affects:** No playbook impact

### v2.1.121
- `alwaysLoad` option in MCP server config (tools skip tool-search deferral). → **Affects:** Power Usage Plugins — **actioned in CBP-084**
- `claude plugin prune` + `plugin uninstall --prune` cascade. → **Affects:** Power Usage Plugins — **actioned in CBP-085**
- PostToolUse hooks can replace tool output via `hookSpecificOutput.updatedToolOutput` for all tools (was MCP-only). → **Affects:** Hooks Exit Codes section — **actioned in CBP-086**
- `CLAUDE_CODE_FORK_SUBAGENT=1` now works in non-interactive sessions. → **Affects:** Subprocess Sandboxing env vars — deferred to next run
- OpenTelemetry: `stop_reason`, `gen_ai.response.finish_reasons`, `user_system_prompt` (gated by `OTEL_LOG_USER_PROMPTS`) added. → **Affects:** OTEL table — deferred to next run
- `/skills` type-to-filter search box. → **Affects:** No playbook impact (UX improvement)
- Multiple bug fixes. → **Affects:** No playbook impact

### v2.1.120
- `claude ultrareview [target]` non-interactive CI/scripts subcommand (`--json`, exit 0/1). → **Affects:** Cheat Sheet (`/ultrareview` row) — **actioned in CBP-082**
- `${CLAUDE_EFFORT}` skill template variable. → **Affects:** Building Skills frontmatter — **actioned in CBP-083**
- `AI_AGENT` env var set for subprocesses. → **Affects:** Subprocess Sandboxing env vars — deferred
- Multiple bug fixes. → **Affects:** No playbook impact

## Playbook Action Items

### Executed This Run (cap = 5)

- [x] **CBP-082** — Updated `/ultrareview` Cheat Sheet row with `claude ultrareview [target]` non-interactive subcommand; updated `/resume` row with PR URL paste-to-find-session feature
- [x] **CBP-083** — Added `${CLAUDE_EFFORT}` to the Building Skills frontmatter String substitutions note
- [x] **CBP-084** — Added `alwaysLoad` MCP server option with code example to Power Usage Plugins collapsible
- [x] **CBP-085** — Added `claude plugin prune` and `plugin uninstall --prune` to Power Usage Plugins collapsible
- [x] **CBP-086** — Added `hookSpecificOutput.updatedToolOutput` PostToolUse output replacement to Hooks Exit Codes section

### Deferred to Next Run

- `CLAUDE_CODE_FORK_SUBAGENT=1` now works in non-interactive SDK sessions — add to Subprocess Sandboxing env vars table
- `AI_AGENT` env var set for subprocesses (gh attribution) — add to Subprocess Sandboxing env vars table
- OTEL: `stop_reason`, `gen_ai.response.finish_reasons`, `user_system_prompt` (gated by `OTEL_LOG_USER_PROMPTS`) — update OTEL events table
- `claude_code.at_mention` OTEL log event — update OTEL events table
