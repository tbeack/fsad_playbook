# 2026-06-04 — Claude Code v2.1.162

## New Features
- `claude agents --json` now includes `waitingFor` showing what a waiting session is blocked on (e.g. permission prompt) → **Affects:** Cheat Sheet (claude agents row)
- `/effort` now confirms when your chosen level will persist as the default for new sessions → **Affects:** Cheat Sheet (/effort row)
- Clicking a slash command in the autocomplete menu fills it into the prompt (press Enter to run) → **Affects:** No playbook impact (behavioral UX only, existing autocomplete note sufficient)
- Remote Control now shows as a persistent footer pill (with a link to the session) instead of a startup message → **Affects:** Power Usage (Remote Control collapsible)

## Changed
- Renamed Windsurf to Devin Desktop in `/ide` menu, `/terminal-setup`, and `/scroll-speed` → **Affects:** Cheat Sheet (/terminal-setup row), Codex IDE extensions table
- `--tools`: explicitly listing Grep/Glob now provides the dedicated search tools on native builds → **Affects:** No playbook impact (behavioral edge case)
- Improved background service startup and `claude update` verification to wait out endpoint-security scanning → **Affects:** No playbook impact (internal)

## Fixed
- Fixed silent startup hang when config directory is read-only or unwritable → **Affects:** No playbook impact
- Fixed WebFetch permission rules not applied to preapproved domains → **Affects:** No playbook impact (permission system internal)
- Fixed Windows permission rules not matching backslash paths → **Affects:** No playbook impact (platform-specific fix)
- Fixed MCP per-server timeout config below 1000ms being floored to 1-second watchdog → **Affects:** No playbook impact (internal fix)
- Fixed LSP tool `workspaceSymbol` returning no results → **Affects:** No playbook impact (bug fix)
- Fixed `claude agents` status text truncation at 60–120 columns → **Affects:** No playbook impact (display fix)
- Fixed `claude agents` Ctrl+V image paste → **Affects:** No playbook impact (UI fix)
- Fixed cross-session `SendMessage` silently breaking with deep `CLAUDE_CODE_TMPDIR` → **Affects:** No playbook impact (internal fix)
- Fixed replies from agents view that fail to send being lost → **Affects:** No playbook impact (resilience fix)
- Quieter startup: notices group by severity, session info and announcements share a single line → **Affects:** No playbook impact (UX polish)

## Playbook Action Items
- [x] CBP-223: Update `claude agents --json` row to document `waitingFor` field — Update existing (Cheat Sheet)
- [x] CBP-224: Update `/effort` row to note new-session default persistence confirmation — Update existing (Cheat Sheet)
- [x] CBP-225: Update Remote Control collapsible: footer pill instead of startup message — Update existing (Power Usage)
- [x] CBP-226: Rename Windsurf → Devin Desktop in /terminal-setup row and IDE extensions table — Update existing (Cheat Sheet + Codex)
