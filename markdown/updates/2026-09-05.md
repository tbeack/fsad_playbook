# 2026-09-05 — Claude Code v2.1.261 / Codex rust-v0.153.4

## Claude Code v2.1.261 (was v2.1.260)

### New Features
- Added `/skill-doctor` slash command — shows which loaded skills go unused and what they cost in context → **Affects:** Cheat Sheet (slash commands table)
- Added `bashOutputMaxChars` and `taskOutputMaxChars` settings — raise inline command/background-task output up to 128K characters → **Affects:** Notable settings.json Keys
- Added `--append-subagent-system-prompt-file` CLI flag — read subagent system prompt from a file for prompts too large for the command line → **Affects:** CLI Launch Flags table
- Added "Organization policy" line to `/status` and `claude doctor` → **Affects:** No playbook impact (informational)
- [VSCode] Added "Build a custom style" walkthrough to Output styles menu → **Affects:** No playbook impact (IDE-only)
- [VSCode] Added Add server / Remove action to MCP servers dialog → **Affects:** No playbook impact (IDE-only)

### Changed
- Changed prompt word-editing keys to match Bash by default: `Ctrl+W` deletes to whitespace, `Alt+F`/`Alt+D` stop at word end, punctuation separates words; `keybindingFlavor` no longer has any effect → **Affects:** Notable settings.json Keys (line 649) and Keyboard Shortcuts table (line 1850)
- Changed auto mode: link packing content into public diagram renderer URL now treated as upload → **Affects:** No playbook impact (informational security note)
- Changed `/context` token counting to use local estimate when token-counting API is unavailable → **Affects:** No playbook impact

### Fixed
- Fixed typed/pasted characters occasionally landing out of order or being dropped during fast input → **Affects:** No playbook impact
- Various Remote Control, SDK, cloud session fixes → **Affects:** No playbook impact

### Improved
- Improved `/model` picker: shows model name instead of raw Bedrock/Vertex/gateway ID → **Affects:** No playbook impact

### Claude Code Playbook Action Items
- [x] `CBP-524` Add `/skill-doctor` slash command row to Cheat Sheet — New row after `/doctor`
- [x] `CBP-525` Add `bashOutputMaxChars` / `taskOutputMaxChars` to Notable settings.json Keys — New entries after `managedMcpServers`
- [x] `CBP-526` Add `--append-subagent-system-prompt-file` to CLI Launch Flags table — New row after `--append-system-prompt`
- [x] `CBP-527` Update `keybindingFlavor` entries to note the setting is now deprecated — Update existing content at two locations

## Codex rust-v0.153.3 and rust-v0.153.4 (was rust-v0.153.2)

### New Features (rust-v0.153.3)
- Added GPT-6-Astra to the Amazon Bedrock model picker for Mantle and Runtime global/US routes → **Affects:** Codex Bedrock section and provider table

### Bug Fixes (rust-v0.153.4)
- Fixed Astra's visibility in bundled model picker; made it the default when no model is explicitly configured → **Affects:** Codex Bedrock section (update existing prose)
- Updated Astra async-question guidance to use supported tool only when available → **Affects:** No playbook impact (internal guidance)

### Codex Playbook Action Items
- [x] `CBP-528` Document GPT-6-Astra on Amazon Bedrock in the Codex provider table and Bedrock collapsible — Update existing content

## Follow-up (same day, PR #217 gap)

`/status`/`claude doctor` "Organization policy" line and the auto-mode public diagram-renderer link rule were assessed above as no playbook impact (informational). On review, one more v2.1.261 item was missed by both passes:

- Improved the dangerous-`rm` safety prompt to also catch `rm -rf` on positional parameters and inside double-quoted `sh -c` scripts → **Affects:** Subprocess Sandboxing paragraph, which already documents the v2.1.246 malformed `&&`/`||` fix in the same style (Update existing)

- [x] `CBP-529` Extend Subprocess Sandboxing paragraph — dangerous-`rm` prompt catches positional params and quoted `sh -c` scripts (v2.1.261)
