# 2026-05-11 — Claude Code v2.1.138

## New Features
- `settings.autoMode.hard_deny` — Auto mode classifier rules that block unconditionally, regardless of user intent or allow exceptions. **Affects:** Claude Best Practices > Config Cascade (Notable settings.json Keys)
- `CLAUDE_CODE_ENABLE_FEEDBACK_SURVEY_FOR_OTEL` — Re-enables the session quality survey for enterprises that are capturing responses through OpenTelemetry (survey was disabled by default when OTEL is active). **Affects:** Claude Best Practices > Monitoring (Quick Start / enterprise OTEL setup)

## Changed
- v2.1.138: Internal fixes only — No playbook impact
- v2.1.137: VSCode extension Windows activation fix — No playbook impact
- v2.1.136 (continued): Extensive bug fixes (MCP reconnect, login loop, plan mode, WSL2, hooks cache, dialogs, shortcuts, etc.) — No playbook impact for most; new features documented above

## Fixed
- Many UX and stability fixes in v2.1.136 — No playbook impact

## Removed
- Nothing removed

## Playbook Action Items
- [ ] CBP-128: Add `settings.autoMode.hard_deny` to Notable settings.json Keys callout in Config Cascade section
- [ ] CBP-129: Add `CLAUDE_CODE_ENABLE_FEEDBACK_SURVEY_FOR_OTEL` env var note to Monitoring section
