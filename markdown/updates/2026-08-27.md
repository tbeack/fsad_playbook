# 2026-08-27 — Claude Code v2.1.247 (Codex CLI unchanged at rust-v0.149.1)

## Claude Code Changes

### New Features / Changed (v2.1.247)
- Added the `SendFeedback` tool: when something goes wrong in a session, Claude can draft a feedback report for you to review and send from `/feedback` (turn off with the `feedbackDrafts` setting).
- Added `{id, text, cooldownSessions, priority}` entries, `tipsFile`, and `label` to `spinnerTipsOverride`, so organizations can rotate their own tips alongside the built-in ones.
- Added a tip on Bash permission prompts pointing to auto mode, with a one-keystroke "Yes, and switch to auto mode" option.
- Added `/claude-api cost-optimize` to profile an existing project's Claude API spend and work through cost levers (caching, token hygiene, batch, effort, model choice) one measured change at a time.
- Updated the `/claude-api` skill with Admin API coverage (organization members, invites, workspaces, API keys, rate limit reports, workload identity federation, CMEK).
- Fixed sub-agents dying on a first-call model 404: they now use the session's fallback model chain, and the error returned to the parent includes the error type, status, request id, and model.
- Changed Sonnet 5's default auto-compact window to its full 1M context, so sessions on the 1M window now auto-compact at about 967K tokens instead of about 934K.
- Changed cross-session peer messages to collapse by default to a one-line `Message from @<sender>: <first line>` preview; Ctrl+O expands the full body.

### Fixed (v2.1.247 — no action, internal reliability/bug fixes)
The remainder of the v2.1.247 "Fixed" list consists of internal reliability, UI, and platform-specific bug fixes with no new user-facing workflow to describe and no existing playbook claim they contradict:
- Fast arrow-key + Enter sequences acting on the wrong row in history search / `/config` / `/mcp` / `/skills` / background tasks / `/model` — UI bug fix, nothing to document.
- A hook or background agent printing megabytes of error output overflowing the conversation and wedging the session — internal safeguard, no documented workflow it changes.
- Ctrl keyboard shortcuts not firing under non-Latin keyboard layouts in kitty-protocol terminals — terminal compatibility fix.
- Mouse-report escape sequences leaking into the prompt — terminal input bug fix.
- Bash sandbox cleanup deleting a dotfile-managed `~/.claude/settings.json` symlink (nix/home-manager, stow) — internal sandbox bug fix.
- `/terminal-setup` overwriting a Zed `keymap.json` instead of merging — terminal-setup bug fix.
- `/rename` silently confirming when the session registry could not be updated — error-messaging fix.
- `/compact` / "Summarize from here" under `--agent` sessions summarizing under the wrong system prompt — internal bug fix.
- `claude agents` "opening…" hang after a terminal host process died — reliability fix.
- Unbounded memory growth when a hook/background task's output file could not be written — internal safeguard.
- `/install-github-app` over SSH copy-shortcut messaging — messaging accuracy fix.
- Foreground shell commands logging internal errors when finishing in background sessions — internal bug fix.
- Version-less marketplace plugin cache directory churn on second-scope install — internal caching bug fix.
- Remote Control sessions not reporting working-tree diff — reliability fix.
- Self-hosted runner sessions reporting `running` prematurely — notification-timing fix.
- First-run setup failing under managed Claude apps gateway sign-in with unreachable Anthropic endpoints — setup reliability fix.
- Cloud sessions showing stale permission mode right after switching — UI sync fix.
- Cloud sessions going silent after a container restart mid-turn — reliability fix.
- Plugin marketplace hardening against control/invisible characters in names — internal input-validation hardening, not a documented workflow change.
- Bedrock/Vertex/Foundry (and telemetry-disabled) sessions now told when a configured MCP server failed to connect — internal diagnostics improvement, no existing doc claim it contradicts.
- Terminal hyperlinks pointing at network/automounter paths or containing control characters rendering as plain text — security-hardening UI fix.
- Prompt-footer PR badge skipping its GitHub re-check on terminal refocus within a minute — internal performance tweak.
- Analytics staying off from startup under managed-gateway/custom-OAuth configs — internal privacy-default fix.
- Claude apps gateway sign-in requests identifying Claude Code via `surface`/User-Agent — internal telemetry detail.
- Organization sign-in enforcement exiting at start when managed settings can't be read — internal enterprise-compliance fix.

## Playbook Action Items
- [x] CBP-472: Document `/feedback`, `SendFeedback` tool, and `feedbackDrafts` setting (Cheat Sheet + Notable settings.json Keys) (v2.1.247)
- [x] CBP-473: Document `spinnerTipsOverride` in Notable settings.json Keys (v2.1.247)
- [x] CBP-474: Add `/claude-api cost-optimize` Cheat Sheet row + Admin API coverage note (v2.1.247)
- [x] CBP-475: Extend Agent Teams cross-session `SendMessage` bullet — peer messages collapse to a one-line preview (v2.1.247)
- [x] CBP-476: Extend `fallbackModel` bullet — sub-agents use the fallback chain on a first-call 404 (v2.1.247)
- [x] CBP-477: Extend Context Management bullets — Sonnet 5 auto-compact window widened to its full 1M context (v2.1.247)

## Deferred
None.

## Execution Gaps
None. All 6 CBP tasks (CBP-472 through CBP-477) resolved to an applied edit in `src/pages/practices.html`, built via `scripts/build-source.py` and `scripts/build-dist.py`.

---

# 2026-08-27 — follow-up run: Codex CLI rust-v0.150.1 (Claude Code unchanged at v2.1.247)

## Codex CLI Changes (rust-v0.150.0 + rust-v0.150.1)

### New Features (rust-v0.150.0, 2026-08-26)
- Reference other Codex tasks with `@` mentions, and ask agents to read, create, or message tasks from the terminal (TUI task tools). (#40308, #40315)
- `/copy` now offers a picker for full responses, individual code blocks, and blockquotes. (#39997)
- Unnamed terminal tasks receive descriptive titles automatically, and `/rename` suggests an editable title based on the conversation. (#40492, #40495)
- Markdown links appear as clickable labels in supported terminals, with visible URLs retained elsewhere. (#40471)
- Bind shortcuts to cycle permission modes, and use `.` in Vim mode to repeat your last edit. (#39873, #40521)
- New `Interrupt` hooks can run commands or MCP handlers when an active top-level turn is interrupted. (#40511)

### Fixed / Changed (rust-v0.150.0)
- Untrusted projects no longer supply project-level `AGENTS.md` instructions, and managed deny-read rules remain enforced after permission changes. (#39837, #40004) — the AGENTS.md half is a documented-behavior change worth reflecting in the playbook's trust/anatomy sections.
- Improved credential redaction in app-server diagnostics — internal, no action.
- Fixed remote MCP bearer-token lookup and required-server startup — internal, no action.
- Fixed elevated Windows sandbox setup and launch aliases under Unicode user paths — internal, no action.
- Prevented Unix shutdown hangs from detached processes — internal, no action.
- Fixed conversation compaction and multi-agent compatibility for Amazon Bedrock models — internal, no action.

### Fixed (rust-v0.150.1, 2026-08-27)
- Remote compaction now counts retained images toward its token budget by default, trimming older images as needed. (#41003) — internal compaction fix, no action.

### OpenAI developers changelog (entries since last check)
- 2026-08-26 — ChatGPT for iOS 1.2026.230: iOS app features (task search, reasoning gauge, prompt editor, Home Screen shortcuts) — ChatGPT app, not Codex CLI; no playbook action (Codex Best Practices page scopes to the `codex` CLI).
- 2026-08-24/25 entries (mcp-server deprecation, browser extensions, event-triggered tasks) — already assessed in prior runs (CBP-470 / 2026-08-26 report); no re-action.

## Playbook Action Items
- [x] CBP-479: [Codex] Document `@` task mentions and terminal task tools (read/create/message tasks) (rust-v0.150.0)
- [x] CBP-480: [Codex] Update `/copy` row (response target picker) and add `/rename` row + automatic descriptive thread titles (rust-v0.150.0)
- [x] CBP-481: [Codex] Document permission-mode-cycling keybindings and Vim dot-repeat (rust-v0.150.0)
- [x] CBP-482: [Codex] Add `Interrupt` hook event — hooks table row + event counts (8 → 9) (rust-v0.150.0)
- [x] CBP-483: [Codex] Untrusted projects no longer load project-level AGENTS.md — trust paragraph + hierarchy note (rust-v0.150.0)

## Deferred
None.
