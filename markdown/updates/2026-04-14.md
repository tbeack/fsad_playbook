# Claude Code Update Report — 2026-04-14

## New Version: v2.1.107 (previously v2.1.98)

---

## Changelog Summary (v2.1.99 – v2.1.107)

### v2.1.107
- Show thinking hints sooner during long operations

### v2.1.105 (Major)
#### New Features
- `path` parameter added to `EnterWorktree` tool — switch into an existing worktree
- **PreCompact hook blocking**: hooks can now block compaction by exiting with code 2 or returning `{"decision":"block"}`
- **Background monitor support for plugins** via top-level `monitors` manifest key — auto-arms at session start or skill invoke
- `/proactive` is now an alias for `/loop`

#### Improvements
- Skill description listing cap raised from 250 → **1,536 characters**; startup warning when truncated
- `/doctor` layout improved with status icons; press `f` to have Claude fix reported issues
- `/config` labels and descriptions clarified
- Stalled API streams now abort after 5 min + retry non-streaming
- `WebFetch` strips `<style>` and `<script>` contents from fetched pages

#### Fixes
- Multiple rendering, MCP, and plugin bugs fixed (see full CHANGELOG)

### v2.1.101
#### New Features
- `/team-onboarding` command — generate a teammate ramp-up guide *(already in playbook)*
- OS CA certificate store trusted by default (enterprise TLS proxy support)
- `/ultraplan` auto-creates default cloud environment

#### Improvements
- Brief mode retries on plain-text responses
- Rate-limit retry messages show which limit was hit and reset time
- `claude -p --resume <name>` accepts session titles set via `/rename`

---

## Playbook Impact Assessment

| Change | Playbook Coverage | Action |
|---|---|---|
| Skill description cap 250 → 1,536 | Line 3259 says "Truncated at 250 chars." — **outdated** | **Update** → CBP-027 |
| PreCompact hook blocking (exit 2 / `{"decision":"block"}`) | PreCompact in event table but blocking not documented | **Update** → CBP-028 |
| `/proactive` alias for `/loop` | `/loop` in cheat sheet, no alias mentioned | **Update** → CBP-029 |
| `monitors` manifest key for plugins | Not in Skills frontmatter table | **Add** → CBP-030 |
| `/team-onboarding` command | Already at line 3852 | No action |
| `path` param for `EnterWorktree` | EnterWorktree not in playbook (internal tool detail) | No action |
| `/doctor` press `f` to fix | Cheat sheet entry is brief — enhancement useful | No action (minor) |
| OS CA cert store trust | Enterprise plumbing, out of scope | No action |
| `/ultraplan` auto-create env | `/ultraplan` not in playbook | No action (not documented) |
| Thinking hints sooner | Internal UX, no doc needed | No action |

## Tasks Created
- CBP-027: Update skill description cap from 250 to 1,536 chars
- CBP-028: Document PreCompact hook blocking behavior
- CBP-029: Add /proactive alias for /loop
- CBP-030: Add `monitors` manifest key to Skills frontmatter reference
