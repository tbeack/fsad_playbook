# 2026-07-04 — Claude Code v2.1.201

## New Features
- None

## Changed
- `AskUserQuestion` dialogs no longer auto-continue by default; opt into an idle timeout via `/config` → **Affects:** No direct playbook section (AskUserQuestion not covered); no playbook action
- "default" permission mode officially renamed to "Manual" across CLI, `--help`, VS Code, JetBrains; `--permission-mode manual` and `"defaultMode": "manual"` accepted alongside the old `default` value → **Affects:** Cheat Sheet — Permission Modes card, Shift+Tab keyboard shortcut row, `--permission-mode` flag row, and cycle caption

## Fixed (v2.1.200)
- Fixed crash at startup when `disabledMcpServers` or `enabledMcpServers` set to non-array → **Affects:** No playbook impact
- Fixed background sessions silently stopping mid-turn after sleep/wake → **Affects:** No playbook impact
- Fixed background-agent daemon handover; stale lock PID reuse → **Affects:** No playbook impact
- Fixed subagents cut off by rate limit returning empty result → **Affects:** No playbook impact
- Fixed control bytes from background-agent output reaching the terminal → **Affects:** No playbook impact
- Fixed `claude agents --plugin-dir <dir>` not showing agents/skills when flag placed after `agents` → **Affects:** No playbook impact
- Fixed project-scoped plugins not loading from git worktrees → **Affects:** No playbook impact
- Fixed `/mcp` server list not tracking focus for screen readers → **Affects:** No playbook impact
- Fixed voice dictation "Voice connection failed" misleading message → **Affects:** No playbook impact
- Fixed rendering flicker under tmux 3.4+ → **Affects:** No playbook impact
- Improved screen-reader output → **Affects:** No playbook impact
- Improved install script to explain OOM kill → **Affects:** No playbook impact

## Changed (v2.1.201)
- Claude Sonnet 5 sessions no longer use mid-conversation system role for harness reminders → **Affects:** Internal behavior; no playbook impact

## Playbook Action Items
- [x] `CBP-296` Update Permission Modes section: rename "Default" card to "Manual", update Shift+Tab cycle row, `--permission-mode` flag description, and cycle caption to use "manual" instead of "default" (v2.1.200)
