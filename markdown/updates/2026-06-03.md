# 2026-06-03 — Claude Code v2.1.161

## New Features
- None this release

## Changed
- `OTEL_RESOURCE_ATTRIBUTES` values are now included as labels on metric datapoints, enabling slicing by custom dimensions like team or repo → **Affects:** Monitoring / Enterprise Configuration (existing OTEL_RESOURCE_ATTRIBUTES mention needs clarification)
- `/mcp` now collapses claude.ai connectors you've never signed in to behind a "Show unused connectors" row → **Affects:** Cheat Sheet `/mcp` row
- Parallel tool calls: a failed Bash command no longer cancels other calls in the same batch — each tool returns its own result independently → **Affects:** Power Usage (notable behavior change for multi-tool usage)

## Fixed
- `forceLoginOrgUUID`/`forceLoginMethod` policies blocking third-party provider sessions (Bedrock, Vertex, Foundry, Mantle) — regression fix → **No playbook impact**
- Background subagent output corrupting `claude -p` stdout — bug fix → **No playbook impact**
- `/usage-credits` re-login for Team/Enterprise admins — bug fix → **No playbook impact**
- `/autofix-pr` false "cannot run on default branch" in git worktrees — bug fix → **No playbook impact**
- `--resume` picker missing sessions from non-git-worktree directories — bug fix → **No playbook impact**
- Windows hooks with explicit bash path failing — bug fix → **No playbook impact**
- OTEL log events dropped before telemetry initialization — bug fix → **No playbook impact**
- `claude mcp` list/get/add printing secrets — security fix → **No playbook impact** (behavior improvement, no config change)
- Workflow agents with `isolation: "worktree"` blocked from editing their own worktree — bug fix → **No playbook impact**
- Background sessions using stale model from daemon environment — bug fix → **No playbook impact**
- Various rendering/crash fixes → **No playbook impact**

## Removed
- None this release

## Playbook Action Items
- [x] CBP-217 Update OTEL_RESOURCE_ATTRIBUTES monitoring note: values now labels on metric datapoints
- [x] CBP-218 Update /mcp Cheat Sheet row: collapses unused claude.ai connectors
- [x] CBP-219 Add parallel tool call independence note to Power Usage
