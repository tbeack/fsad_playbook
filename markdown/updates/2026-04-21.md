# 2026-04-21 — Claude Code v2.1.116

## Releases Checked
- v2.1.113 (2026-04-17) — significant release
- v2.1.114 (2026-04-18) — crash fix only, no playbook impact
- v2.1.116 (2026-04-20) — significant release (no v2.1.115 tag)

## New Features

- `Ctrl+U` now clears entire input buffer (v2.1.111, not yet reflected in playbook) — **Affects:** Cheat Sheet keyboard shortcuts
- `Ctrl+Y` restores the cleared buffer — **Affects:** Cheat Sheet keyboard shortcuts
- `sandbox.network.deniedDomains` setting (v2.1.113): block specific domains even when `allowedDomains` wildcard permits them — **Affects:** Power Usage / Subprocess Sandboxing
- `/terminal-setup` now configures VS Code / Cursor / Windsurf scroll sensitivity in addition to Shift+Enter (v2.1.116) — **Affects:** Cheat Sheet config table
- `/loop` Esc now cancels pending wakeups (v2.1.113) — **Affects:** Power Usage / /loop collapsible
- Agent frontmatter `hooks:` fires when running via `--agent` (v2.1.113 + v2.1.116) — **Affects:** Skills frontmatter reference table

## Changed

- Native binary spawning (v2.1.113) — **No playbook impact** (internal)
- Thinking spinner inline progress (v2.1.116) — **No playbook impact** (UI)
- `/config` search matches values (v2.1.116) — **No playbook impact** (minor UX)
- `/doctor` works mid-response (v2.1.116) — **No playbook impact** (minor UX)
- `/reload-plugins` auto-installs dependencies (v2.1.116) — **No playbook impact** (minor)
- Bash tool `gh` rate-limit hint (v2.1.116) — **No playbook impact** (internal)
- v2.1.114: agent team permission crash fix — **No playbook impact**

## Fixed

- Various terminal rendering and keyboard fixes — **No playbook impact**

## Playbook Action Items

- [x] CBP-049 — Batch update: keyboard shortcuts, sandbox settings, /terminal-setup, /loop, hooks frontmatter
