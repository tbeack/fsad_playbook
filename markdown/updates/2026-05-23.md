# 2026-05-23 — Claude Code v2.1.150

## New Features
- None in this release window (v2.1.149–v2.1.150).

## Changed
- **`/usage` per-category breakdown** — `/usage` now shows a per-category breakdown of what's driving limits usage: skills, subagents, plugins, and per-MCP-server cost → **Affects:** Cheat Sheet (Model/Mode/Usage table)
- **`/diff` keyboard scrolling** — the `/diff` detail view can now be scrolled with the keyboard (arrows, `j`/`k`, `PgUp`/`PgDn`, `Space`, `Home`/`End`) → **Affects:** Cheat Sheet (Utilities table)
- **GFM task list checkboxes** — markdown output now renders `- [ ] todo` / `- [x] done` as checkboxes instead of plain bullets → No playbook action (rendering detail, not a workflow change)
- **`allowAllClaudeAiMcps` managed setting** — enterprise setting to load claude.ai cloud MCP connectors alongside `managed-mcp.json` → No playbook action (enterprise-only managed setting)
- **`/feedback` improvement** — now includes pre-compaction conversation for better triage → No playbook impact

## Fixed
- Fixed PowerShell permission bypass via built-in `cd` functions → No playbook impact
- Fixed sandbox write allowlist in git worktrees covering entire main repo root instead of shared `.git` only → No playbook impact
- Fixed `find` in the Bash tool exhausting macOS file/vnode table on large directory trees → No playbook impact
- Fixed `/ultraplan` and remote session creation failing with "Could not capture uncommitted changes" when no real changes exist → No playbook impact
- Fixed thinking spinner staying amber across tool calls → No playbook impact
- Fixed collapsed Bash output reporting wrong hidden-line count → No playbook impact
- Fixed slash-command argument-hint clipping trailing typed characters → No playbook impact
- Fixed status bar showing baseline `/effort` setting instead of skill/agent applied effort → No playbook impact
- Many other internal bug fixes → No playbook impact
- v2.1.150: Internal infrastructure improvements only → No playbook impact

## Playbook Action Items
- [x] CBP-187 — Update `/usage` Cheat Sheet row: add per-category breakdown (skills, subagents, plugins, MCP servers)
- [x] CBP-188 — Update `/diff` Cheat Sheet row: add keyboard navigation in detail view
