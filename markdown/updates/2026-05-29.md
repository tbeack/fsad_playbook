# 2026-05-29 — Claude Code v2.1.156

## New Features
- Dynamic workflows GA + `/workflows` command to view runs → **Affects:** Cheat Sheet (automation table)
- `claude agents`: type `! <command>` or `claude --bg --exec '<command>'` to run shell commands as background sessions → **Affects:** Cheat Sheet (claude agents row)
- Stdio MCP server subprocesses now receive `CLAUDE_CODE_SESSION_ID` and `CLAUDECODE=1` → **Affects:** Power Usage (Subprocess Sandboxing / Hooks env vars)
- Opus 4.8 is now default on Max/Team Premium (already covered by CBP-200) → No playbook impact

## Changed
- `/simplify` reverted to cleanup-only review (reuse, simplification, efficiency, altitude); no longer invokes `/code-review --fix` → **Affects:** Cheat Sheet (`/code-review` row — reversal of CBP-192)
- Effort slider labels renamed from "Speed"/"Intelligence" to "Faster"/"Smarter" → **Affects:** No direct playbook content (UI label only); deferred
- `/simplify` now runs cleanup-only and applies fixes (separate from `/code-review --fix`) → **Affects:** Cheat Sheet
- `claude agents` `←←` shortcut to open agents view now works on Bedrock/Vertex/Foundry → No action needed
- Lean system prompt is now default for all models except Haiku/Sonnet/Opus ≤4.7 → Already documented as `--exclude-dynamic-system-prompt-sections`; No playbook impact

## Fixed
- v2.1.156: Fixed Opus 4.8 thinking block modification causing API errors → No playbook impact
- Many bug fixes across both releases → No playbook impact

## Deferred (noted for next run)
- `/chrome` browser picker (Claude in Chrome — pick connected browser)
- `defaultEnabled: false` plugin manifest key
- `/plugin` Discover directory annotation ("suggested for this directory")
- `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` deprecation notice (removed 06/01)
- Effort slider label rename "Faster"/"Smarter" (UI only, no code impact)

## Playbook Action Items
- [x] CBP-202: Fix `/code-review` row — remove stale `/simplify` invokes `/code-review --fix` note
- [x] CBP-203: Add `/workflows` to Cheat Sheet automation table
- [x] CBP-204: Update `claude agents` row with `! <command>` and `--bg --exec`
- [x] CBP-205: Update `CLAUDE_CODE_SESSION_ID` env var row — also injected into stdio MCP servers; add `CLAUDECODE=1`
