# 2026-04-17 — Claude Code v2.1.112

## New Features (v2.1.111)
- `xhigh` effort level for Opus 4.7 (between `high` and `max`; falls back to `high` on other models) → **Affects:** Cheat Sheet, Power Usage
- `/effort` interactive slider — no-arg call now opens arrow-key slider instead of printing help → **Affects:** Cheat Sheet, Power Usage
- "Auto (match terminal)" theme option via `/theme` → **Affects:** Cheat Sheet
- `/less-permission-prompts` built-in skill — scans transcripts and proposes allowlist → **Affects:** Cheat Sheet
- `/ultrareview [PR#]` — cloud-based parallel multi-agent code review → **Affects:** Cheat Sheet
- Auto mode no longer requires `--enable-auto-mode` flag → **Affects:** No playbook impact (flag wasn't documented)
- PowerShell tool via `CLAUDE_CODE_USE_POWERSHELL_TOOL` → **Affects:** No playbook impact (platform-specific, no section for Windows tooling)
- `OTEL_LOG_RAW_API_BODIES` env var for full API body logging → **Affects:** No playbook impact (niche debugging tool)

## Changed (v2.1.111)
- Plan files now named after your prompt instead of random words → **Affects:** No playbook impact
- `Ctrl+U` now clears full input buffer; `Ctrl+Y` restores → **Affects:** No playbook impact (not documented in cheat sheet)
- `/skills` menu supports sorting by token count (`t` key) → **Affects:** No playbook impact

## Fixed (v2.1.112)
- Fixed "claude-opus-4-7 is temporarily unavailable" for auto mode → **Affects:** No playbook impact

## Playbook Action Items
- [x] CBP-046: Add `xhigh` effort level + interactive slider docs
- [x] CBP-047: Update `/theme`, add `/ultrareview`, add `/less-permission-prompts`
