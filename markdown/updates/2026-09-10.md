# 2026-09-10 — Claude Code v2.1.265 → v2.1.267 + Codex rust-v0.154.0

## Claude Code v2.1.265 Changes (implemented this run: CBP-530 through CBP-534)

### New Features
- `--plugin-dir` now supports a folder of plugins: each child folder with a `plugin.json` loads automatically; children added/removed while running are picked up (hot-reload) → **Affects:** Claude Best Practices — Power Usage → Plugins
- `/workflows` agent detail improved: tool calls marked running/failed/done, subagent task list shown, Enter unfolds calls with inputs and results → **Affects:** Claude Best Practices — Cheat Sheet
- Slash commands typed mid-prompt now show as a scrollable list (Tab opens outside fullscreen); plugin skills findable by bare name without `plugin:` prefix → **Affects:** Claude Best Practices — Prompt Tips
- 1 GB cap on tool results saved to disk; in-conversation preview notes truncation → **Affects:** Claude Best Practices — Power Usage → Context Management
- [VS Code] Sessions inactive for a configurable period auto-archived (default 14 days); adjustable via "Archive inactive sessions" setting → **Affects:** Claude Best Practices — Power Usage → Remote Control & Cross-Device

### Claude Code Playbook Action Items (completed)
- [x] CBP-530 — Update `--plugin-dir` to document folder-of-plugins with hot-reload
- [x] CBP-531 — Update `/workflows` cheat sheet row with agent detail improvements
- [x] CBP-532 — Update `/commands` card text with slash command list behavior
- [x] CBP-533 — Add 1 GB tool result disk cap to Context Management collapsible
- [x] CBP-534 — Add VS Code auto-archive inactive sessions note

## Claude Code v2.1.266 (no playbook impact)

A single regression fix: the undocumented `CLAUDE_CODE_USE_GATEWAY` environment variable was forcing Cloud-gateway sign-in in v2.1.265 even when an API key or `apiKeyHelper` was set; this is now fixed. No documentation change needed.

## Claude Code v2.1.267 Changes (queued for next run — 5-task cap reached)

### New Features
- `maxEffortLevel` setting (top-level or per model under `modelSettings`): caps the effort level on every provider including Bedrock, Vertex, and Foundry → **Affects:** Claude Best Practices — Power Usage / Notable Settings
- `--system-prompt-snapshot off`: renders the system prompt fresh on every request instead of reusing the recorded prompt, useful for iterating on prompt text → **Affects:** Claude Best Practices — Cheat Sheet CLI flags

### Changed
- `effort:` frontmatter on custom commands/skills/subagents now respected on models with a pinned default effort (Opus 4.7, 4.8, Fable 5) → **Affects:** Claude Best Practices — Skills section
- Many VS Code bug fixes (cyclic parent link CPU hang, CRLF diff accepts, RTL text input, etc.) → **No playbook impact**
- Various prompt-cache stability improvements and session resume fixes → **No playbook impact**

### Queued Action Items (for next run)
- [ ] Add `maxEffortLevel` setting to Notable `settings.json` Keys section
- [ ] Add `--system-prompt-snapshot off` CLI flag to Cheat Sheet

## Codex rust-v0.154.0 Changes (queued for next run — 5-task cap reached)

### New Features
- Experimental worktree support: create isolated checkouts for new or forked sessions using `--worktree` or `/worktree`, browse and resume them → **Affects:** Codex Best Practices
- Answer questions inline while Codex continues working — suggested choices or custom text without losing the main draft → **Affects:** Codex Best Practices
- Windows sessions can now share a background Codex server with daemon lifecycle commands and managed updates → **Affects:** Codex Best Practices (Windows note)
- `/copy` can now copy status output or individual session fields → **Affects:** Codex Best Practices — Cheat Sheet

### Removed
- `codex mcp-server` entry point deprecated and removed → **Affects:** Codex Best Practices (if documented)

### Queued Action Items (for next run)
- [ ] Document worktree support (`--worktree` / `/worktree`) in Codex Best Practices
- [ ] Document inline Q&A while working in Codex Best Practices
- [ ] Check if `codex mcp-server` is mentioned and remove/deprecate if so
