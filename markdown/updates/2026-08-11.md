# 2026-08-11 — Claude Code v2.1.228 (Codex CLI: no new release)

## Claude Code Changes

Covers v2.1.225 through v2.1.228 (previous checked version: v2.1.224).

### New Features
- Added gateway spend-limit support to Claude Code's usage warning; the limit-reached message now names the cap, its reset time, and the operator's message (requires the gateway on 2.1.225) (v2.1.225)
- Added a workspace trust prompt to `claude agents` for untrusted directories, matching the behavior of `claude` (v2.1.225)
- SendMessage can now start a conversation with your Remote Control sessions on other machines by name (`ListAgents` shows them as `name [ref]`), instead of only replying after they message you first (v2.1.225)

### Changed
- Hardened skills synced from claude.ai: they no longer shadow local commands or MCP prompts, their descriptions are sanitized and labeled, and on your machine their bodies don't run `!` commands or expand `@` files (v2.1.228)
- Changed the Write tool so newer models can overwrite an existing file they haven't read this session, matching the Edit tool's rules; older models still require the read first (v2.1.228)
- Improved cross-session messages: the sender and body now display inline instead of a collapsed line, and messages to Remote Control sessions on other machines show your Remote Control session name as the sender (v2.1.228)
- Improved Vertex AI credential handling: expired or missing Google Cloud credentials now fail within seconds instead of retrying for minutes (v2.1.228)
- Improved compaction progress: the retry countdown and stall hint now appear during compaction instead of only a progress bar (v2.1.228)
- Updated terminal title busy-spinner glyphs to reduce tab-bar jitter on some terminals (v2.1.228)
- Removed the outdated note about auto mode sessions costing slightly more from the first-use notice for Pro, Max, and Team plans (v2.1.228)
- Improved slash-command menu: blue now marks only the selected row, matched characters are bolded instead of recolored, and emoji or accented names keep their glyphs (v2.1.227)
- Improved performance: fewer event-loop stalls on file-not-found suggestions and at-mention size checks (v2.1.227)
- SendMessage: a Remote Control recipient you already confirmed is never swapped for a same-named session on this machine when its own list couldn't be checked (v2.1.225)
- Improved Remote Control: photos attached from the Claude app are now shown to Claude directly instead of being read from disk with a separate tool call (v2.1.225)
- v2.1.226: Bug fixes and reliability improvements (no changelog detail provided upstream)

### Fixed
- Fixed interactive sessions that could stop redrawing entirely, while the process kept running, after a rare internal layout error (v2.1.228)
- Fixed `git` / Git Bash not being found on Windows when Claude Code is launched from a parent folder of the git installation (v2.1.228)
- Fixed `/tui` reverting the session to an earlier model when `/model` had been changed since the last response (v2.1.228)
- Fixed cross-session messaging sometimes starting without an inbox in the first session after install or upgrade (v2.1.228)
- Fixed Remote Control `/resume` while connected leaking the resumed conversation's title or history into the connected session (v2.1.228)
- Fixed `claude self-hosted-runner` sessions failing on every fresh runner when the `checkout` hook fails for a repository the session doesn't push to; that repository is now skipped with a warning (v2.1.228)
- Fixed self-hosted runners ending sessions in the gap between a background task finishing and the follow-up turn starting (v2.1.228)
- Fixed session cleanup deleting contents inside a project's memory folder (v2.1.228)
- Fixed background plugin-cache cleanup deleting a plugin's cache when its only version is a symlinked development checkout (v2.1.228)
- Fixed a settings-merge issue where a marketplace entry redefined in a higher-precedence settings tier could inherit another tier's custom headers; marketplace entries now merge as whole entries (v2.1.228)
- Fixed the deferred-tools reminder occasionally being sent to the model twice after a skill invocation (v2.1.228)
- Fixed feature flags being evaluated without the user's subscription tier when a session started with an expired login token, which could wrongly prompt Max plan users to enable usage credits for Fable (v2.1.227)
- Fixed every Bash command failing under `claude-code-action` with `allowed_non_write_users` on GitHub-hosted runners (v2.1.227)
- Fixed `/tui` bringing back a conversation that had been rewound to before its first message (v2.1.227)
- Fixed a transient 401 replacing a long-lived `CLAUDE_CODE_OAUTH_TOKEN` with a stored login's short-lived token, breaking headless sessions until restart (v2.1.225)
- Fixed MCP OAuth servers on macOS intermittently failing with a burst of 401 errors, as if never authenticated, after a keychain read timed out (v2.1.225)
- Fixed auto mode counting a safety-filter refusal of its own permission check toward the consecutive-block limit; the action is still denied, but the model is now told to move on rather than retry (v2.1.225)
- Fixed cross-session messages staying parked without a notice or expiry in headless sessions and during startup (v2.1.225)
- Fixed conversation history breaking on Remote Control session resume after very large conversations were compacted (v2.1.225)
- Fixed hovering over a session in another project in the agents list changing the directory the next agent starts in (v2.1.225)
- Fixed `claude self-hosted-runner` registering and then failing every session when `--base-dir` cannot be created or written; it now exits at startup with a clear error (v2.1.225)
- Fixed Claude Code on the web sessions being misreported as stuck, re-sending a growing event backlog on every reconnect (v2.1.225)
- [VSCode] Fixed Focus view folding away the latest to-do list, a pending question's context, and settled answers; thinking-only folds show "Thought for Ns" and re-collapse when their turn completes (v2.1.225)

## Playbook Action Items
- [x] `CBP-368`: Update Agent Teams cross-session `SendMessage` bullet — v2.1.225 initiate-by-name to Remote Control sessions + `name [ref]` `ListAgents` format + v2.1.228 inline sender/body display
- [x] `CBP-369`: Add Remote Control bullet — mobile photo attachments shown to Claude directly (v2.1.225)
- [x] `CBP-370`: Add Building Skills callout — hardened skills synced from claude.ai (v2.1.228)

All three tasks were executed in Phase 5, verified accurate against the source changelog by the Phase 5.5 refuter agent (second pass, after the first pass caught and this run fixed a misattribution of the `name [ref]` display format — see below), and applied to `fsad-playbook.html`.

## No Action (confirmed by Phase 3 sub-agent sweep, with cited evidence of absence in the relevant section)
- Changed the Write tool so newer models can overwrite an existing file they haven't read this session, matching the Edit tool's rules; older models still require the read first (v2.1.228) — no action, because no anchor group in the playbook documents Write/Edit tool read-before-write internals; confirmed absent in Cheat Sheet, Getting Started, Claude Setup, Power Usage, Building Skills, Hooks Deep Dive, and Best Practices.
- Added a workspace trust prompt to `claude agents` for untrusted directories, matching the behavior of `claude` (v2.1.225) — no action, because the playbook doesn't document trust-prompt UX for either `claude` or `claude agents` anywhere; this is safety parity with no existing precedent to extend.
- Added gateway spend-limit support to the usage warning (v2.1.225) — no action, because it's a narrow enterprise-gateway runtime message with no corresponding config surface documented in Monitoring/Enterprise or elsewhere; confirmed no "spend" hits anywhere in the file.
- SendMessage: a Remote Control recipient you already confirmed is never swapped for a same-named session on this machine when its own list couldn't be checked (v2.1.225) — no action, internal edge-case safety fix with no user-facing behavior to document.
- Improved slash-command menu: blue marks only selected row, matched characters bolded, emoji/accented names preserved (v2.1.227) — no action, cosmetic-only UI polish to an already-generic cheat sheet reference.
- Improved Vertex AI credential handling: fail fast instead of retrying for minutes (v2.1.228) — no action, internal reliability fix with no documented config/workflow to update.
- Improved compaction progress: retry countdown and stall hint during compaction (v2.1.228) — no action, minor UX polish to an existing `/compact` reference with no behavioral change to reflect.
- Removed the outdated note about auto mode session costs from the first-use notice (v2.1.228) — no action, this is an in-app first-use notice, not documented playbook content.
- Fixed `claude self-hosted-runner` sessions failing when a checkout hook fails for a repo the session doesn't push to (v2.1.228) — no action, pure bug fix; the Self-Hosted Runner section doesn't describe the old failure mode.
- Fixed self-hosted runners ending sessions in the gap between background task completion and follow-up turn start (v2.1.228) — no action, pure bug fix with no stale documentation to correct.
- Fixed the deferred-tools reminder occasionally being sent to the model twice after a skill invocation (v2.1.228) — no action, pure bug fix; Building Skills doesn't describe deferred-tools reminder mechanics.
- Fixed a settings-merge issue where a marketplace entry could inherit another tier's custom headers (v2.1.228) — no action, pure bug fix; no playbook section documents marketplace settings-merge semantics.
- Fixed feature flags being evaluated without subscription tier on expired login token (v2.1.227) — no action, pure bug fix with no documented behavior to correct.
- Fixed every Bash command failing under `claude-code-action` with `allowed_non_write_users` on GitHub-hosted runners (v2.1.227) — no action; the Code Review section's `claude-code-action` example doesn't reference `allowed_non_write_users`, so nothing is stale.
- Remaining ~20 bullet-level bug fixes in v2.1.225–v2.1.228 (session redraw hang, Windows git detection, `/tui` model/rewind bugs, cross-session inbox/parking bugs, Remote Control `/resume` leak and conversation-history-on-resume bugs, session-directory hover bug, session cleanup deleting memory folder, plugin-cache cleanup bug, MCP OAuth 401 bursts, auto mode consecutive-block counting, `claude self-hosted-runner --base-dir` startup error, Claude Code on the web misreported-as-stuck bug, VSCode Focus view folding, terminal title glyph tweak) — no action, all are internal reliability/bug fixes with no corresponding documented (and now-stale) behavior anywhere in the playbook.

## Deferred
(none — every actionable change from Phase 3 was turned into a CBP task; see Playbook Action Items above)

## Execution Gaps
(none — all 3 CBP tasks created in Phase 4 resolved to an applied-and-Phase-5.5-verified edit. Phase 5.5's first pass returned FAIL on CBP-368 for a `name [ref]` scoping error; the edit was corrected and a second Phase 5.5 pass returned PASS before proceeding to Phase 6.)
