# 2026-05-17 — Claude Code v2.1.143

## New Features
- Plugin dependency enforcement: `claude plugin disable` refuses when another enabled plugin depends on the target (with a copy-pasteable disable-chain hint); `claude plugin enable` force-enables transitive dependencies → **Affects:** Plugins collapsible (Power Usage)
- Projected context cost (per-turn and per-invocation token estimates) in `/plugin` marketplace browse pane → **Affects:** `/plugin` Cheat Sheet row and Plugins collapsible
- `worktree.bgIsolation: "none"` setting — lets background sessions edit the working copy directly without `EnterWorktree`, for repos where worktrees are impractical → **Affects:** Work Trees collapsible (Power Usage)
- PowerShell tool now passes `-ExecutionPolicy Bypass` by default; opt out with `CLAUDE_CODE_POWERSHELL_RESPECT_EXECUTION_POLICY=1`. Also: `CLAUDE_CODE_USE_POWERSHELL_TOOL=0` to disable; PowerShell tool now enabled by default on Windows for Bedrock, Vertex, and Foundry users → **Affects:** Subprocess Sandboxing env vars table
- `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` env var — stop hooks that block repeatedly now loop at most 8 times before turn ends with warning → **Affects:** Subprocess Sandboxing env vars table and/or Hooks Deep Dive

## Changed
- Background sessions preserve model and effort level after waking from idle → **Affects:** No playbook impact (bug fix / already-documented behavior)
- `claude agents` now accepts `--add-dir`, `--settings`, `--mcp-config`, `--plugin-dir`, `--permission-mode`, `--model`, `--effort`, `--dangerously-skip-permissions` → **Affects:** Already documented in CBP-147 at line 6325, skip
- `/bg` now preserves multiple flags across respawn → **Affects:** No playbook impact (bug fix / internal behavior)
- `NO_COLOR`/`FORCE_COLOR` from `settings.json env` now apply to subprocesses only → **Affects:** No playbook impact (corrected internal behavior)
- Worktree cleanup no longer falls back to `rm -rf` → **Affects:** No playbook impact (safety fix)

## Fixed
- Corrupt `.credentials.json` hanging CLI startup → No playbook impact
- Right-click paste in `claude agents` on Windows Terminal/WSL → No playbook impact
- Esc/Ctrl+C not cancelling pending `/loop` wakeup → No playbook impact
- `/goal` evaluator firing while subagents still running → No playbook impact
- Agent view spawning repeated PowerShell processes on Windows → No playbook impact
- `/bg` without a prompt sending "continue" to forked session → No playbook impact
- `--agent <name>` not finding plugin-contributed agents without `plugin:` prefix → No playbook impact
- Various Windows/WSL/macOS background session fixes → No playbook impact

## Playbook Action Items
- [x] CBP-153 — Plugin dependency enforcement in `claude plugin disable`/`enable` → Update Plugins collapsible
- [x] CBP-154 — Projected context cost in `/plugin` marketplace → Update `/plugin` Cheat Sheet row + Plugins collapsible
- [x] CBP-155 — `worktree.bgIsolation: "none"` setting → Update Work Trees collapsible
- [x] CBP-156 — PowerShell env vars (`CLAUDE_CODE_POWERSHELL_RESPECT_EXECUTION_POLICY`, `CLAUDE_CODE_USE_POWERSHELL_TOOL`) → Add to Subprocess Sandboxing table
- [x] CBP-157 — `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` env var → Add to Subprocess Sandboxing table + Hooks note
