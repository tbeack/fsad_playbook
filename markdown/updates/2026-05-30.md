# 2026-05-30 — Claude Code v2.1.158

## New Features

- Auto mode is now available on Bedrock, Vertex, and Foundry for Opus 4.7 and Opus 4.8. Opt in by setting `CLAUDE_CODE_ENABLE_AUTO_MODE=1` → **Affects:** Power Usage (Subprocess Sandboxing env vars table)

## Changed (v2.1.157)

- Plugins in `.claude/skills` directories are now automatically loaded, no marketplace required → **Affects:** Power Usage (Plugins collapsible)
- Added `claude plugin init <name>` to scaffold a new plugin in `.claude/skills` → **Affects:** Cheat Sheet (`/plugin` row) + Plugins collapsible
- `EnterWorktree` can now switch between Claude-managed worktrees mid-session → **Affects:** Power Usage (Work Trees collapsible)
- Added autocomplete for `/plugin` arguments (subcommands, installed plugin names, known marketplaces) → **Affects:** No playbook impact
- `claude agents`: the `agent` field in `settings.json` is now honored for dispatched sessions, with `--agent <name>` to override → **Affects:** No playbook impact (minor agent dispatch config)
- `tool_decision` telemetry events now include `tool_parameters` (bash commands, MCP/skill names) when `OTEL_LOG_TOOL_DETAILS=1` → **Affects:** Already documented at line 10793; no change needed
- Worktrees managed by Claude are now left unlocked when the agent finishes → **Affects:** No playbook impact (behavior note)
- `/terminal-setup` now disables GPU acceleration in VS Code/Cursor/Windsurf integrated terminals → **Affects:** No playbook impact (minor fix)
- Added "Workflow keyword trigger" setting in `/config` → **Affects:** No playbook impact (minor config option)
- Removed "bash commands will be sandboxed" startup banner → **Affects:** No playbook impact
- Various bug fixes for agents view, worktrees, background sessions, clipboard, IME → **Affects:** No playbook impact

## Fixed

- Fixed unprocessable images crashing the request → **Affects:** No playbook impact
- Fixed sandbox network permission prompts in auto/bypass-permissions mode → **Affects:** No playbook impact
- Numerous other bug fixes → **Affects:** No playbook impact

## Playbook Action Items

- [x] CBP-210: Add `claude plugin init <name>` to Cheat Sheet `/plugin` row and Plugins collapsible code block
- [x] CBP-211: Add plugins auto-load from `.claude/skills` (no marketplace) bullet to Plugins collapsible
- [x] CBP-212: Add `CLAUDE_CODE_ENABLE_AUTO_MODE=1` env var to Subprocess Sandboxing hardening table
- [x] CBP-213: Update Work Trees collapsible to note `EnterWorktree` mid-session worktree switching (v2.1.157)
