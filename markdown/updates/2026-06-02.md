# 2026-06-02 — Claude Code v2.1.160

## New Features / Security
- Added prompt before writing to shell startup files (`.zshenv`, `.zlogin`, `.bash_login`) and `~/.config/git/` → **No playbook impact** (internal safety gate, not user-facing workflow)
- `acceptEdits` mode now prompts before writing build-tool config files that grant code execution (`.npmrc`, `.yarnrc*`, `bunfig.toml`, etc.) → **No playbook impact** (additive safety behavior, no doc change needed)

## Changed
- **Renamed dynamic-workflow trigger keyword from `workflow` to `ultracode`** — the word "workflow" in a prompt no longer triggers a dynamic workflow run; asking for one in your own words still works; the trigger keyword is highlighted violet in the prompt input → **Affects:** Power Usage — Dynamic Workflows section (also supersedes pending CBP-214)
- Edit no longer requires a separate Read after viewing a file with `grep` — single-file grep/egrep/fgrep commands now satisfy read-before-edit → **No playbook impact** (internal tool behavior; no playbook doc change needed)
- **Removed `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE`** env var — now a no-op → **Affects:** Env Vars table in Power Usage / Subprocess Sandboxing section

## Fixed (selected, high-visibility)
- Fixed `claude --bg` socket-missing failures on cold-start → No playbook impact
- Fixed `claude agents` freezing on auto-updater re-check on every exit → No playbook impact
- Fixed vim mode `p` paste behavior → No playbook impact
- Fixed background agents emitting terminal sync-output markers in Apple Terminal / tmux → No playbook impact
- Various background session / agents view fixes → No playbook impact

## Removed
- Removed JetBrains plugin install suggestion from startup → No playbook impact (we don't document this)

## Playbook Action Items
- [x] CBP-214: Update Dynamic Workflows section — document that `ultracode` is the trigger keyword (replacing `workflow`), add `/config` opt-out note and `Alt+W` / Backspace dismissal tip
- [x] CBP-215: Remove deprecated `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` env var row from env vars table
