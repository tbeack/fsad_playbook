# 2026-08-28 — Claude Code v2.1.248 + v2.1.250 (Codex CLI unchanged at rust-v0.150.1)

## Claude Code Changes

### New Features / Changed (v2.1.248)
- Added `--restricted` (or `CLAUDE_CODE_RESTRICTED=1`): removes the built-in tools that run commands or code and `WebFetch` (unless named in `--tools`), keeps file tools inside the working directory, refuses `bypassPermissions`, and ignores user, project and local settings files.
- Added `experimental.cacheTtl` (`"5m"` or `"1h"`) to agent frontmatter: a per-agent prompt cache TTL used when no subagent TTL setting is configured.
- Added `claude self-hosted-runner --client-label <label>` (or `SELF_HOSTED_RUNNER_CLIENT_LABEL`) to override the label the runner registers with (default: hostname).
- Added cross-session messaging (`SendMessage` / `ListAgents`) between sessions on the same machine on Bedrock, Vertex, and Foundry, and when telemetry is disabled.
- Added server-managed settings diagnostics: a startup warning when the settings fail to load, and a `/doctor` and `/status` line explaining a load failure or why they weren't fetched (Bedrock/Vertex/third-party provider, custom `ANTHROPIC_BASE_URL`).
- Added a warning in `/web-setup` when the GitHub CLI token lacks the `workflow` scope, since pushes to very large repositories can be rejected without it.
- Added `/usage-credits` for Enterprise organizations billed through AWS Marketplace, self-serve Enterprise, and Enterprise trials, so members can request a higher usage limit from their admin.
- Changed `/loop`: self-paced dynamic mode and the no-prompt autonomous default are now always available, including on Bedrock/Vertex/Foundry.
- Changed `SendMessage` from a subagent to another session: the result now notes that any reply is delivered to the parent session's conversation, not to the subagent.
- Improved the Workflow tool's prompt footprint: its description is now about 1k tokens instead of 5.7k, with the script-writing reference moved into a bundled `workflow-authoring` skill.

### Fixed (v2.1.248 — no action, internal reliability/bug fixes)
The remainder of the v2.1.248 "Fixed"/"Improved"/"Changed" list consists of internal reliability, UI, and platform-specific bug fixes with no new user-facing workflow to describe and no existing playbook claim they contradict:
- Prompt-cache miss (and lost extended-thinking context) roughly once an hour caused by tool definitions re-rendering after OAuth token refresh — internal fix.
- `ScheduleWakeup` tool definition changing between a session and its `--resume` under usage overage, causing a full prompt-cache miss — internal fix.
- Claude Desktop/Cowork sessions disappearing after 30 days (new `desktopSessionCleanupPeriodDays` setting) — desktop-app-specific, outside CLI-focused playbook scope.
- Being sent to the login screen when another process held the token refresh lock — internal auth fix.
- Windows `claude agents` list keyboard/terminal-mode bugs — platform-specific UI fix.
- Recommended Console sign-in failing before showing a URL under API-key configs — sign-in fallback fix.
- Model names rendering as links instead of code in `/model`/fast-mode notices — cosmetic fix.
- `claude agents` workspace-trust-prompt/PR-cache/session-resurrection/second-terminal/stopped-session bugs — internal `claude agents` reliability fixes.
- Background sessions waiting silently on invalid hook JSON output — error-surfacing fix, no new documented workflow.
- `/mcp` claude.ai-connector-type entries listed under the wrong heading — internal MCP listing fix.
- MCP `headersHelper` Authorization-header OAuth-discovery fallback bug — internal MCP auth fix.
- `/login` hang under managed-settings security approval — internal sign-in fix.
- Gateway model discovery never running with only `apiKeyHelper` configured — internal fix.
- `claude logs` leaving terminal modes switched on — terminal cleanup fix.
- Trust dialog emoji-truncation garbling — cosmetic fix.
- Permission mode indicator hidden behind the Ctrl-C exit hint — cosmetic fix.
- `/ultrareview` and locally seeded cloud sessions uploading uncommitted secrets-adjacent files (`prod.env`, `*.tfvars`, editor swap/temp/backup credential copies) — security-hardening fix, no existing playbook claim to update (no prior documented behavior asserted these were ever uploaded).
- Remote Control permission-prompt/latest-message sync bug after silent reconnect — internal RC fix.
- Cloud sessions failing at startup on unready container credentials — internal fix.
- `claude remote-control` rejecting its own flags when preceded by another option — CLI-parsing fix.
- Startup warning column misalignment — cosmetic fix.
- Backgrounded worktree session losing its checkout during cleanup — internal worktree-lock fix.
- `@`-mentions not matching non-Latin-script session names (e.g. Korean via IME) — internal input-handling fix.
- Invalid `crossSessionInbound` value being silently ignored — now warns/holds/refuses instead — internal validation fix, no existing playbook claim it contradicts.
- Rate-limit/usage/fast-mode messages recommending `/usage-credits` when unavailable for the org — messaging-accuracy fix.
- VSCode chat tab stuck on "No conversation found" — IDE-extension-specific fix.
- Managed-settings/stream-watchdog/MCP-startup-mode env vars no longer triggering settings-approval prompt — internal approval-flow fix.
- `/ultrareview <PR#>` pre-flight GitHub-access check before cloud session start — internal UX improvement, no existing doc claim to update.
- Cross-session messaging `/tmp` fallback directory — internal fallback fix.
- shift+enter/ctrl+enter dispatch-input key remap in the agent view — UI behavior change, dashboard-specific.
- Anthropic telemetry export failures logged at debug level under a distinct prefix — internal logging change.
- Cross-session messaging root-equivalent trust scoping in Linux user namespaces — internal security hardening, no existing doc claim to update.

### v2.1.250 (2026-08-28)
- "Bug fixes and reliability improvements" — no changelog detail provided; nothing to document.

## Playbook Action Items
- [x] CBP-484: Document `--restricted` / `CLAUDE_CODE_RESTRICTED=1` in the Subprocess Sandboxing hardening env vars table (v2.1.248)
- [x] CBP-485: Document `experimental.cacheTtl` agent frontmatter in the Prompt Caching TTL table (v2.1.248)
- [x] CBP-486: Extend Agent Teams cross-session `SendMessage`/`ListAgents` bullet — Bedrock/Vertex/Foundry/telemetry-disabled support + subagent-reply routing note (v2.1.248)
- [x] CBP-487: Extend Self-Hosted Runner section — `--client-label` / `SELF_HOSTED_RUNNER_CLIENT_LABEL` (v2.1.248)
- [x] CBP-488: Extend `/loop` section — self-paced dynamic mode + no-prompt autonomous default always available, including Bedrock/Vertex/Foundry (v2.1.248)
- [x] CBP-489: Extend `/usage-credits` Cheat Sheet row — Enterprise AWS Marketplace/self-serve/trials can request a higher usage limit (v2.1.248)

## Deferred
None.

## Execution Gaps
None. All 6 CBP tasks (CBP-484 through CBP-489) resolved to an applied edit in `src/pages/practices.html`, built via `scripts/build-source.py` and `scripts/build-dist.py`.
