# 2026-05-08 — Claude Code v2.1.133

## New Features
- Added `worktree.baseRef` setting (`fresh` | `head`) to control whether `--worktree`, `EnterWorktree`, and agent-isolation worktrees branch from `origin/<default>` (fresh) or local `HEAD` (head). Default is `fresh`. → **Affects:** Power Usage (Work Trees collapsible)
- Added `sandbox.bwrapPath` and `sandbox.socatPath` managed settings (Linux/WSL) for custom bubblewrap/socat binary paths → **Affects:** Power Usage (Subprocess Sandboxing settings table)
- Added `parentSettingsBehavior` admin-tier key (`first-wins` | `merge`) for SDK `managedSettings` policy merge — enterprise/admin niche → **Affects:** No playbook impact (admin-tier setting beyond playbook scope)
- Hooks now receive active effort level via `effort.level` JSON input field; Bash tool commands can read `$CLAUDE_EFFORT` env var → **Affects:** Hooks Deep Dive (environment variables table)

## Changed
- Improved focus mode behavior → No playbook impact
- Improved memory usage by releasing warm-spare background workers under memory pressure → No playbook impact

## Fixed
- Fixed parallel sessions dead-ending at 401 after refresh-token race → No playbook impact
- Fixed Edit/Write allow rules scoped to drive root or POSIX / matching incorrectly → No playbook impact
- Fixed subagents not discovering project, user, or plugin skills via the Skill tool → No playbook impact (bug fix)
- Fixed /effort in one session changing effort of other concurrent sessions → No playbook impact (bug fix)
- Various other bug fixes → No playbook impact

## Playbook Action Items
- [x] CBP-125: Add `effort.level` JSON input field and `$CLAUDE_EFFORT` env var to hooks env vars table (v2.1.133)
- [x] CBP-126: Add `worktree.baseRef` setting to Work Trees collapsible (v2.1.133)
- [x] CBP-127: Add `sandbox.bwrapPath` and `sandbox.socatPath` to Subprocess Sandboxing settings table (v2.1.133)

## Codex
Codex is unchanged at rust-v0.129.0 (last updated 2026-05-07). No Codex tasks this run.
