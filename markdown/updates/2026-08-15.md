# 2026-08-15 — Claude Code v2.1.233

## Claude Code Changes

### New Features
- Added GitLab merge request URL support to the `--worktree` flag and the `claude agents` view (MRs display as `!N`)
- Added an opt-in `forward_user_identity` apps gateway setting on Anthropic upstreams that sends the signed-in user's identity as headers, so a proxy behind the gateway can attribute spend per user
- Added opt-in memory cgroup support for Bash tool commands on Linux (`CLAUDE_CODE_TOOL_MEMORY_LIMIT`) so a runaway build can't stall the session
- Added `CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS` environment variable to configure the WebFetch session URL cache TTL (default unchanged: 15 minutes)

### Changed
- Todo/task-tracking tools (TaskCreate/Get/Update/List, TodoWrite) are no longer available on Opus 4.8, Sonnet 5, Fable 5, Mythos 5, and newer models by default; set `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` to bring them back
- Changed the GitHub app setup tip to no longer appear in repositories whose origin remote is on gitlab.com or bitbucket.org; the enterprise marketplace tip now covers non-GitHub internal git hosts
- Improved `claude plugin validate` to check a bare `.claude/skills` directory, reporting SKILL.md files whose frontmatter fails to parse
- Improved `claude self-hosted-runner` session start time: the session branch is now created without rewriting the working tree, and two server round trips no longer block the agent's launch
- Improved apps gateway error forwarding: 400/413 errors from Vertex, Foundry, and Claude Platform on AWS upstreams now carry the upstream's own message; fixes a bug with auto-compact on apps gateway
- Improved screen reader mode: the `/effort` selector renders as a numbered list with a typed-number prompt, and hint and dialog text is no longer clipped
- Improved print mode diagnostics: a `[claude-code:unrecognized_model]` line is written to stderr when a request goes out for a model ID Claude Code doesn't recognize; map it with `modelOverrides` to silence

### Fixed
- Fixed cloud sessions occasionally being marked as lost when the environment shut down while Claude was waiting on a permission prompt
- Fixed MCP v2 connections endlessly reopening the subscriptions/listen stream against servers that terminate long-held streams on a fixed timeout (e.g. serverless hosts)
- Fixed Notification hooks not firing for permission prompts when running under Claude Desktop or VS Code
- Fixed idle sessions on Linux sometimes keeping one CPU core at 100% when sandboxing is enabled
- Fixed bundled skill aliases like `/checkup` and `/review` reporting "Unknown command" in `-p` mode or with plugins/MCP loaded when a user or project skill shadows the bundled skill
- Fixed skill/command argument substitution to prevent argument values from being re-expanded as template markers
- Fixed Windows paths spelled with the NT `\??\` device prefix bypassing UNC path validation, closing an NTLM credential-leak vector
- Windows: fixed auto mode repeatedly stopping for manual approval on ordinary `cd <dir> && <command> > file` Bash commands (a 2.1.232 regression)
- Reverted the 2.1.232 Bash permission changes for Cygwin-style symlinks on Windows and for input redirections (`< file`); a narrower version will return in a later release

## Playbook Action Items
- [ ] `CBP-376`: Add GitLab MR URL support note to `--worktree` flag and `claude agents` row in Cheat Sheet
- [ ] `CBP-377`: Add `CLAUDE_CODE_TOOL_MEMORY_LIMIT` env var to Power Usage Hardening env vars table
- [ ] `CBP-378`: Add `CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS` env var to Power Usage Hardening env vars table
- [ ] `CBP-379`: Add Todo/task-tracking tools default-off callout to Best Practices Workflow subsection

## No Action (assessed, not actionable)
- `forward_user_identity` apps gateway setting — no action, because it's an enterprise apps-gateway proxy config with no dedicated gateway-configuration section in the playbook.
- Improved apps gateway error forwarding (Vertex/Foundry/AWS upstreams) — no action, because it's an infra-level bug fix with no corresponding playbook content to update.
- Fixed cloud sessions occasionally marked as lost — no action, pure bug fix restoring already-correct intended behavior.
- Fixed MCP v2 connections endlessly reopening subscriptions/listen stream — no action, pure bug fix; MCP coverage in Integrations is basic setup only.
- Fixed Notification hooks not firing under Claude Desktop/VS Code — no action, pure bug fix; hooks table already lists Notification correctly, nothing describes the broken behavior as correct.
- Fixed idle sessions on Linux keeping a CPU core at 100% — no action, internal performance bug fix.
- Fixed bundled skill aliases (`/checkup`, `/review`) reporting "Unknown command" under shadowing — no action, pure bug fix restoring already-documented intended shadowing behavior.
- Fixed skill/command argument substitution re-expansion bug — no action, pure bug fix; substitution syntax is already documented correctly.
- Fixed Windows NT `\??\` device prefix path-validation bypass — no action, internal security bug fix, not a documented workflow.
- Improved `claude self-hosted-runner` session start time — no action, internal perf improvement; self-hosted-runner capability is already documented at the appropriate level (CBP-355) and this doesn't change user-facing behavior.
- Improved screen reader mode (`/effort` numbered-list rendering, clipped text fixes) — no action, accessibility UI polish; existing screen-reader settings/flag docs are unaffected and this is too granular for the guide.
- Improved print mode diagnostics (`[claude-code:unrecognized_model]` stderr line) — no action, narrow CI/debugging diagnostic, too internal for this playbook's audience.
- Changed GitHub app setup tip scoping away from gitlab.com/bitbucket.org origins — no action, CLI UX polish with no corresponding guidance section to update.
- Improved `claude plugin validate` to check bare `.claude/skills` directories — no action, QA/tooling improvement; Building Skills only mentions `claude plugin validate` in passing with no dedicated validation-behavior section to update.
- Windows: fixed auto mode repeatedly stopping for manual approval on `cd && command > file` (v2.1.232 regression) — no action, pure regression bug fix, Windows-specific.
- Reverted 2.1.232 Bash permission changes for Cygwin symlinks / input redirections — no action, pure bug-fix reversion.
- `[claude-code:unrecognized_model]` diagnostic is covered above; no separate Codex changes this run (Codex CLI unchanged at rust-v0.147.0; no dev-changelog entries newer than that release).

## Deferred

## Execution Gaps
