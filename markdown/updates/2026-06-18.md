# 2026-06-18 — Claude Code v2.1.181

## New Features
- Added `/config key=value` syntax to set any setting from the prompt (e.g. `/config thinking=false`) — works in interactive, `-p`, and Remote Control → **Affects:** Cheat Sheet (Claude Best Practices)
- Added `sandbox.allowAppleEvents` opt-in setting for sandboxed commands to send Apple Events on macOS → **Affects:** Notable settings.json Keys callout (Claude Best Practices)
- Added `CLAUDE_CLIENT_PRESENCE_FILE` environment variable: point it at a marker file to suppress mobile push notifications while at the machine → **Affects:** Hardening env vars table (Claude Best Practices)

## Changed
- Upgraded bundled Bun runtime to 1.4 → **Affects:** No playbook impact
- Improved streaming of long paragraphs (text appears line-by-line) → **Affects:** No playbook impact
- Improved auto-retry: API connection drops mid-thinking now auto-retry → **Affects:** No playbook impact
- Improved subagent panel: idle subagents auto-hide after 30s, list caps at 5 rows → **Affects:** No playbook impact (cosmetic)
- Improved MCP OAuth browser page visual style → **Affects:** No playbook impact
- Changed fullscreen mode URL opening to require Cmd+click (macOS) / Ctrl+click → **Affects:** No playbook impact
- Changed `Improved N memories` line to no longer list individual files outside verbose mode → **Affects:** No playbook impact

## Fixed
- Fixed foreground subagents spawning unbounded nested chains; now respect the same 5-level depth limit as background subagents → **Affects:** Already documented correctly at line 10168 (CBP-245) — No action
- Fixed `/recap` and conversation forks using previous model after model switch → **Affects:** No playbook impact
- Various other bug fixes (startup, prompt caching, macOS, AWS, MCP, clipboard) → **Affects:** No playbook impact

## Playbook Action Items
- [ ] CBP-258: Update `/config` Cheat Sheet row to document `key=value` inline syntax
- [ ] CBP-259: Add `sandbox.allowAppleEvents` to sandbox settings table
- [ ] CBP-260: Add `CLAUDE_CLIENT_PRESENCE_FILE` to hardening env vars table
