# 2026-05-15 — Claude Code v2.1.142

## New Features
- Added new `claude agents` dispatch flags: `--add-dir`, `--settings`, `--mcp-config`, `--plugin-dir`, `--permission-mode`, `--model`, `--effort`, `--dangerously-skip-permissions` → **Affects:** Cheat Sheet (claude agents row)
- Fast mode now defaults to Opus 4.7 (was 4.6); `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE=1` pins to 4.6 → **Affects:** Subprocess Sandboxing env vars table
- Plugins with a root-level `SKILL.md` and no `skills/` subdirectory are now surfaced as a skill → **Affects:** Plugins collapsible (Power Usage)
- `/plugin` details pane and `claude plugin details` now show LSP servers a plugin provides → **Affects:** Cheat Sheet `/plugin` row, Plugins collapsible

## Changed
- `/web-setup` warns before replacing an existing GitHub App connection → **Affects:** No playbook impact (not documented)
- Improved reactive compaction: first summarize attempt seeds from original overflow size → **Affects:** No playbook impact (internal)
- Improved hook configuration error for prompt/agent-type hooks on SessionStart/Setup/SubagentStart → **Affects:** No playbook impact (UX message improvement only)

## Fixed
- `MCP_TOOL_TIMEOUT` now raises per-request fetch timeout for remote HTTP/SSE MCP servers → **Affects:** No playbook impact (bugfix)
- Background sessions not recognizing pre-existing git worktrees → **Affects:** No playbook impact (bugfix)
- Background sessions disappearing after macOS sleep/wake → **Affects:** No playbook impact (bugfix)
- Various daemon, agents, plugin, and MCP bugfixes → **Affects:** No playbook impact

## Removed
- Stale `/model claude-sonnet-4-20250514` suggestion removed from Usage Policy refusal messages → **Affects:** No playbook impact

## Playbook Action Items
- [x] CBP-147: Update `claude agents` Cheat Sheet row with new dispatch flags
- [x] CBP-148: Add `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` to Subprocess Sandboxing env vars table
- [x] CBP-149: Document root-level SKILL.md plugin surfacing in Plugins collapsible
- [x] CBP-150: Update `/plugin` Cheat Sheet row and Plugins collapsible with LSP server display
