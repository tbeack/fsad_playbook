# 2026-05-25 — Claude Code v2.1.150

## New Features
- `/usage` per-category breakdown: skills, subagents, plugins, and per-MCP-server cost → **Affects:** Cheat Sheet (`/usage` row)
- `/diff` detail view keyboard scrolling: arrows, `j`/`k`, `PgUp`/`PgDn`, `Space`, `Home`/`End` → **Affects:** Cheat Sheet (`/diff` row)

## Changed
- Pinned background sessions in `claude agents` view (`Ctrl+T`): stay alive when idle, restart in place on updates, shed last under memory pressure → **Affects:** Cheat Sheet (`claude agents` row)
- `/simplify` renamed to `/code-review` with new `--comment` flag to post inline GitHub PR comments → **Affects:** Cheat Sheet (`/code-review` row) — `--comment` was missing
- Markdown output now renders GFM task list checkboxes (`- [ ]` / `- [x]`) → **No playbook impact**
- Enterprise: `allowAllClaudeAiMcps` managed setting → **No playbook impact** (enterprise-only)
- Improved diff rendering performance for large file edits → **No playbook impact**
- Prompt history no longer records consecutive duplicate entries → **No playbook impact**
- Improved auto-updater: retries transient failures, reports error categories → **No playbook impact**

## Fixed
- PowerShell permission bypass (`cd..`, `cd\`, `cd~`, `X:`) → **No playbook impact**
- Sandbox write allowlist in git worktrees → **No playbook impact**
- `find` exhausting macOS file/vnode table → **No playbook impact**
- `/ultraplan` failing with "Could not capture uncommitted changes" → **No playbook impact**
- Bash tool exit code 127 regression (v2.1.148) → **No playbook impact**
- Various UI and session fixes in v2.1.149 → **No playbook impact**
- Various PowerShell and hook fixes in v2.1.147 → **No playbook impact**

## Removed
- None

## Playbook Action Items
- [ ] CBP-185: Update `/usage` Cheat Sheet row — add per-category breakdown (skills, subagents, plugins, MCP) note
- [ ] CBP-186: Update `/diff` Cheat Sheet row — document keyboard scrolling in detail view
- [ ] CBP-187: Update `/code-review` Cheat Sheet row — add `--comment` flag for inline GitHub PR comments
- [ ] CBP-188: Update `claude agents` Cheat Sheet row — document `Ctrl+T` pinning in agents view
