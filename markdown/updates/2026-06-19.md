# 2026-06-19 — Claude Code v2.1.183

## New Features
- Added `attribution.sessionUrl` setting to omit the claude.ai session link from commits and PRs in web and Remote Control sessions → **Affects:** Config Cascade / Notable settings.json Keys
- Added `/config --help` to list all available shorthand keys for `/config key=value` → **Affects:** Cheat Sheet (`/config` row)
- Improved auto mode safety: destructive git commands and IaC destroy commands are now blocked by default unless explicitly requested → **Affects:** Config Cascade / Notable settings.json Keys

## Changed
- `/config` toggle behavior: Enter and Space both change the selected setting; Esc now saves and closes instead of reverting → **Affects:** No playbook impact (UI behavior detail)
- Added warning when requested model is deprecated or auto-updated to newer model (stderr in `-p` mode, also covers agent frontmatter) → **Affects:** No playbook impact (informational runtime behavior)
- Removed startup "setup issues" line under the logo — run `/doctor` instead → **Affects:** No playbook impact (UI removal)

## Fixed
- `thinking.disabled.display` 400 errors on subagent spawns and session-title generation → No playbook impact
- WebSearch returning empty results in subagents → No playbook impact
- Terminal cursor stranded after history navigation in vim mode → No playbook impact
- Fullscreen TUI corruption in Windows Terminal under heavy nested-subagent load → No playbook impact
- Turns completing silently when model returned only a thinking block → No playbook impact
- User-level skills appearing multiple times in slash-command autocomplete → No playbook impact
- MCP servers requiring auth exposing auth-stub tools in headless/SDK mode → No playbook impact
- tmux teammate pane launch failures and keystroke leakage → No playbook impact
- Background tasks killed when teammate finishes a turn → No playbook impact
- Scheduled task/webhook trigger deliveries classified as keyboard input → No playbook impact
- Focus mode showing "Ran N PostToolUse hooks" timing lines → No playbook impact

## Playbook Action Items
- [x] CBP-261: Add `attribution.sessionUrl` to Notable settings.json Keys callout
- [x] CBP-262: Add auto mode built-in safety blocks note to Notable settings.json Keys callout
- [x] CBP-263: Update `/config` Cheat Sheet row to mention `--help` flag
