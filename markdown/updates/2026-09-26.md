# 2026-09-26 — Claude Code v2.1.283

Claude Code: v2.1.282 → v2.1.283.

## Claude Code v2.1.283

### New Features

- `/doctor prompt-audit` (also `/checkup prompt-audit`) audits CLAUDE.md files, skills, agents, and commands for prompting patterns written for older models → **Affects:** `/doctor` Cheat Sheet row
- `availableModelsMatch: "exact"` managed setting — an `availableModels` entry allows only the exact model version named, so new releases stay blocked until listed → **Affects:** Notable Settings list (new bullet, alongside `enforceAvailableModels`)
- `deniedModels` managed setting — blocks specific models even when `availableModels` allows them → **Affects:** Notable Settings list (same new bullet)
- `x-claude-code-prompt-id` gateway hint header (opt-in via `CLAUDE_CODE_GATEWAY_HINT_HEADERS=1`), `tool.output` added to OTEL `tool.output` span event, `load_test_mode` gateway config block, `mantle` Bedrock upstream provider → No playbook impact (enterprise gateway/observability internals outside current doc scope)
- Click-to-expand for truncated messages from other sessions in fullscreen mode, `path` added to `--plugin-dir` load-failure entries → No playbook impact (minor UI/debugging details)

### Changed

- `--system-prompt`/`--append-system-prompt` now accept their text and `-file` forms together in the same invocation; the file's text comes first → **Affects:** System prompt & config table (`--system-prompt-file`/`--append-system-prompt-file` rows)
- `Skill(anthropic-skills:<name>)` deny rules also block Claude Desktop plugin-delivered skills; `Skill(skill:<name>)` denies match alias and display name → No playbook impact (permission-rule syntax not currently covered at this level of detail)
- `--plugin eval` requires git 2.31+; artifact auto-arm watch now expires after 3.5h idle; `/rewind`/`/diff`/`/workflows` list keybinding/sizing consistency changes → No playbook impact (internal tooling/UX polish)
- Reverted the 2.1.282 `claude-ai` name reservation for skills/commands/workflows/MCP → No playbook impact (bug fix reverting an unreleased-in-docs restriction)

### Fixed / Removed

- Large batch of reliability fixes: SDK session tool-call/result loss on early turn end, MCP progress notifications during backgrounding, stdio MCP cleanup on early session end, stateless remote MCP 404 handling, MCP sign-in error messaging, `/model` id-suffix matching, `DISABLE_PROMPT_CACHING_HAIKU`, several `claude plugin` validation/listing bugs → No playbook impact (internal reliability, not user-configurable behavior)
- VS Code, Cloud sessions, Claude Tag (Slack), and Code Review fixes (permission-mode indicator, teleported-session messages, routine scheduling note, review retry/billing) → No playbook impact (IDE/platform-specific bug fixes outside this playbook's scope)

### Claude Playbook Action Items

- [x] CBP-604 Add `/doctor prompt-audit` / `/checkup prompt-audit` note to Cheat Sheet
- [x] CBP-605 Update `--system-prompt-file`/`--append-system-prompt-file` rows — combinable with text forms
- [x] CBP-606 Add `availableModelsMatch` / `deniedModels` managed settings to Notable settings.json Keys

---

## Codex Check — 2026-09-26

npm registry shows `@openai/codex` at `0.157.1` (previously tracked as `rust-v0.156.1`), indicating a new release. **This session's GitHub access is scoped to `tbeack/fsad_playbook` only** — `github.com` and `api.github.com` requests to `openai/codex` (releases page, releases API, releases Atom feed) were refused ("GitHub access to this repository is not enabled for this session"), and `openai/codex`'s `CHANGELOG.md` only points to the (inaccessible) releases page rather than listing entries inline. No Codex release-note content could be fetched, so no relevance assessment or CBP tasks were produced for it this run. `state.json`'s `lastCodexVersion`/`lastCodexChecked` are left unchanged so a future run with release-notes access re-attempts a real assessment rather than this one silently marking Codex "checked."
