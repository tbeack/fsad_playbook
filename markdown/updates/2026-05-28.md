# 2026-05-28 — Claude Code v2.1.153

## New Features (v2.1.149–v2.1.153)

### v2.1.153
- `skipLfs` option for `github`/`git` plugin marketplace sources → No playbook impact (internal/plugin authoring edge case)
- npm global install auto-update notice + `/doctor` fixes → **Affects:** Cheat Sheet (`/doctor` row)
- Status line commands receive `COLUMNS`/`LINES` env vars → No playbook impact (advanced scripting)
- `claude agents` autocomplete suggests native slash commands + bundled skills → No playbook impact (UX improvement)
- `claude agents` PR column `PR #N` vs `N PRs` → No playbook impact (cosmetic)
- `claude doctor` shows last update attempt result → No playbook impact (minor enhancement already covered)
- `/model` now saves as default for new sessions; `s` to switch session-only → **Affects:** Cheat Sheet (CBP-167 row is stale; needs update)
- `modelPicker:thisSessionOnly` keybinding rename → **Affects:** any keybinding documentation
- macOS background agents appear as "Claude Code" in Privacy & Security → No playbook impact (fix)
- Various bug fixes → No playbook impact

### v2.1.152
- `/code-review --fix` applies review findings to working tree; `/simplify` now invokes `/code-review --fix` → **Affects:** Cheat Sheet `/code-review` row (stale)
- `disallowed-tools` frontmatter field for skills/slash commands → **Affects:** Skills frontmatter reference table (new field)
- `/reload-skills` command added → **Affects:** Cheat Sheet (new command to add)
- `SessionStart` hooks: `reloadSkills: true` + `hookSpecificOutput.sessionTitle` → **Affects:** Hooks Deep Dive (SessionStart row)
- `MessageDisplay` hook event → **Affects:** Hooks Deep Dive (new hook event row)
- `pluginSuggestionMarketplaces` managed setting → No playbook impact (enterprise admin)
- `claude plugin marketplace remove --scope` flag → No playbook impact (minor flag extension)
- `--fallback-model` for session recovery → No playbook impact (CLI flag edge case)
- Auto mode no longer requires opt-in consent → No playbook impact (behavior change, self-explanatory)
- Vim mode `/` in NORMAL mode → No playbook impact (vim power user)
- Various rendering and bug fixes → No playbook impact

### v2.1.150
- Internal infrastructure improvements → No playbook impact

### v2.1.149
- `/usage` per-category breakdown (skills, subagents, plugins, per-MCP-server cost) → No playbook impact (minor enhancement)
- `/diff` scrollable with keyboard → No playbook impact (UX improvement)
- GFM task list checkboxes rendered → No playbook impact (markdown rendering)
- `allowAllClaudeAiMcps` managed setting → No playbook impact (enterprise admin)
- Various bug fixes → No playbook impact

## Playbook Action Items
- [x] CBP-187 — Update `/model` Cheat Sheet row to v2.1.153 behavior (`s` for session-only, default-saving restored)
- [x] CBP-188 — Update `/code-review` Cheat Sheet row with v2.1.152 `--fix` behavior
- [x] CBP-189 — Add `MessageDisplay` hook event to Hooks Deep Dive events table
- [x] CBP-190 — Add `/reload-skills` command to Cheat Sheet; add `reloadSkills`/`sessionTitle` to SessionStart hook row
- [x] CBP-191 — Add `disallowed-tools` frontmatter field to Skills frontmatter reference table

## Deferred to Next Run
- `skipLfs` plugin marketplace option (niche authoring detail)
- Status line `COLUMNS`/`LINES` env vars
- `pluginSuggestionMarketplaces` managed setting
- `claude plugin marketplace remove --scope`
- `--fallback-model` CLI flag
- Auto mode opt-in removal note
- `/usage` per-category breakdown enhancement note
