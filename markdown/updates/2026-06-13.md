# 2026-06-13 — Claude Code v2.1.177

## New Features
- Session titles now generated in the language of the conversation; pin via `language` setting → **Affects:** Claude Best Practices (Notable settings.json Keys)
- Added `footerLinksRegexes` setting for regex-matched link badges in the footer row → **Affects:** Claude Best Practices (Notable settings.json Keys)

## Changed
- Improved Bedrock `awsCredentialExport` credential caching (uses actual expiry instead of fixed 1hr) → **Affects:** No playbook impact (internal behavior, no new config key)
- `availableModels` enforcement hardened for alias model picks and `/fast` toggle → **Affects:** No playbook impact (bug fix to existing behavior)
- Remote Control footer indicator shortened to "/rc active" → **Affects:** No playbook impact (already noted as footer pill; change is cosmetic)
- `/effort` confirm behavior (already documented in CBP-224) → **Affects:** No playbook impact

## Fixed
- Hook `if` conditions for Read/Edit/Write path matching now work correctly → **Affects:** No playbook impact (confirms existing documented patterns work)
- Many Remote Control, background session, Linux sandbox, clipboard, and Windows fixes → **Affects:** No playbook impact (reliability fixes)
- `/cd` and worktree git branch reporting fix → **Affects:** No playbook impact

## Playbook Action Items
- [x] CBP-252 — Add `language` setting to Notable settings.json Keys callout (v2.1.176)
- [x] CBP-253 — Add `footerLinksRegexes` setting to Notable settings.json Keys callout (v2.1.176)

Note: v2.1.177 shipped with no public changelog entries (release body was empty). All actionable content is from v2.1.176.
